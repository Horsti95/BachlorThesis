\documentclass{imc-inf}

% --- IMC Template Specific Commands ---
\title{Optimizing ML Experiments: Intelligent Frameworks for Efficiency and Reproducibility}
\subtitle{A Fingerprint-Based LOSO Caching Approach Validated on EEG Sleep Classification}
\thesistype{Bachelor Thesis}
\author{Lennart Gorzel}
\supervisor{Prof. Himanshu Buckchash}
\copyrightyear{2026}
\submissiondate{May 2026} % TODO: Update to actual submission date
\keywords{Machine Learning, Intelligent Caching, Reproducibility, Sleep Stage Classification, EEG, Feature Engineering}

% --- Standard Packages ---
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{svg}
\usepackage{rotating}
\usepackage{afterpage}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{tcolorbox}
\usepackage{algorithmic}
\usepackage{algorithm}

% ---- Colored TODO/Note boxes for thesis review ----
\newtcolorbox{todobox}[1][]{colback=red!8, colframe=red!70!black, fonttitle=\bfseries, title=TODO, #1}
\newtcolorbox{verifybox}[1][]{colback=orange!8, colframe=orange!70!black, fonttitle=\bfseries, title=VERIFY, #1}
\newtcolorbox{claudenote}[1][]{colback=blue!8, colframe=blue!60!black, fonttitle=\bfseries, title=Claude Note, #1}
\newtcolorbox{missingref}[1][]{colback=purple!8, colframe=purple!60!black, fonttitle=\bfseries, title=Missing Reference, #1}
\newtcolorbox{plotsuggestion}[1][]{colback=green!8, colframe=green!60!black, fonttitle=\bfseries, title=Plot/Figure Suggestion, #1}
\newtcolorbox{generatefigure}[1][]{colback=yellow!15, colframe=yellow!60!black, fonttitle=\bfseries, title=Generate This Figure, #1}

\begin{document}

% --- Front Matter ---
\frontmatter
\maketitle

\begin{declarations}
\end{declarations}

% ============================================================================
% ABSTRACT
% ============================================================================

\begin{abstract}
This thesis addresses the computational costs and reproducibility challenges
that confront modern Machine Learning (ML) experimentation. As ML systems grow
increasingly complex, researchers face significant overhead from redundant
computations, inconsistent results across runs, and difficulties resuming
interrupted experiments. This work proposes a fingerprint-based caching
framework using SHA-256 configuration hashing to systematically address these
challenges.

The research develops a deterministic fingerprinting system where a cryptographic
hash uniquely identifies each experiment configuration. The fingerprint is computed
from a canonical JSON representation of the tracked experiment state: random seed,
code version, model type and hyperparameters, feature configuration, and held-out
subject. The core principle is:
\textit{if it can change the output, it must change the fingerprint}. This enables
automatic cache invalidation for all tracked experiment parameters: when any
fingerprinted component changes, the cache key changes and stale results are
bypassed. Identical fingerprints therefore indicate the same tracked configuration
and a valid cache match.

The framework extends caching to Leave-One-Subject-Out cross-validation by
including the held-out subject in the cache key. This prevents reuse of models
across different LOSO folds and avoids cross-contamination between train/test
splits under the fixed dataset partition used in this thesis.

The framework is validated through a sleep stage classification case study using
the Bitbrain Open Access Sleep (BOAS) dataset (128 subjects, approximately
120,000 epochs). The feature extraction module computes 149 features per epoch
across 6 EEG channels (F3, F4, C3, C4, O1, O2), spanning time-domain statistics,
frequency-domain power spectral features, and signal complexity measures. These
features are computed once per subject and cached persistently, isolating the
caching evaluation to the computationally expensive model training phase.

Experimental evaluation across 18 configurations (2 models $\times$ 3 correlation
thresholds $\times$ 3 feature counts) with 128-fold LOSO cross-validation
demonstrates: XGBoost achieves 84.9\% accuracy (Cohen's $\kappa = 0.73$, F1-macro
= 0.63) and Random Forest achieves 81.5\% accuracy ($\kappa = 0.65$, F1-macro =
0.56). The caching framework provides speedups of approximately 67$\times$ for
XGBoost and 12$\times$ for Random Forest on the validated hardware configuration,
with 100\% prediction match between cached and cold-trained models across all
2,304 training runs.

The contributions of this thesis are: (1)~a fingerprint-based LOSO model caching
framework that ensures cache correctness through SHA-256 configuration tracking,
preventing cross-contamination across folds, (2)~quantified efficiency gains
demonstrating model-dependent caching viability---XGBoost benefits substantially
while Random Forest's large serialized model size limits cache throughput, and
(3)~validated reproducibility---all cached models produce identical classification
results to cold retraining (accuracy match confirmed for all configurations).
\end{abstract}

\addtoToC{Table of Contents}%
\tableofcontents%
\clearpage

\addtoToC{List of Tables}%
\listoftables
\clearpage

\addtoToC{List of Figures}%
\listoffigures
\clearpage

% ============================================================================
% MAIN MATTER
% ============================================================================
\mainmatter

% Chapters (5-chapter structure per professor's requirements)
\include{chapters/01_introduction}
\include{chapters/02_background}
\include{chapters/03_related_work}
\include{chapters/04_methodology}
\include{chapters/05_summary}

% ============================================================================
% BACK MATTER
% ============================================================================
\backmatter%
\addtoToC{Bibliography}
\bibliographystyle{IEEEtranS}
\bibliography{references}

% Appendices
\begin{appendices}
\include{chapters/appendix_pipeline_settings}
\end{appendices}

\end{document}
