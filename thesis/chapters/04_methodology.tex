\chapter{Methodology and Results}
\label{chap:methodology}
\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:}
Goal 20 pages currently 31 shorten to at least 25
\end{minipage}}
\end{center}

This chapter follows a quantitative experimental methodology. EEG-based sleep
stage classification serves as the validation vehicle, a domain where LOSO
cross-validation is standard practice, rather than as the primary
contribution. The contribution is the fingerprint-based caching framework
itself; sleep staging provides a realistic, high-volume workload
(128 subjects, 18 configurations, 2{,}304 training operations) on which to
demonstrate and measure it. Section~\ref{sec:data} describes the dataset,
Sections~\ref{sec:preprocessing}--\ref{sec:features} detail the signal
processing and feature extraction pipeline, Section~\ref{sec:caching}
introduces the two-layer caching architecture and the fingerprint
mechanism that drives it, Section~\ref{sec:experiment_setup} outlines the
experimental design, and Section~\ref{sec:evaluation_plan} defines the
evaluation metrics.

\section{Data: BOAS Dataset}

\label{sec:data}

\subsection{Dataset Overview}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_sleep_stage_distribution.pdf}
\caption[Sleep stage distribution across all 128 BOAS subjects]{Sleep stage
epoch distribution across all 128 BOAS subjects (119,759 epochs total).
N2 dominates at 60.3\% and N1 is severely underrepresented at 3.7\%,
reflecting the natural structure of healthy adult sleep.}
\label{fig:sleep_distribution}
\end{figure}


This research utilizes the Bitbrain Open Access Sleep (BOAS) dataset, a
polysomnographic database designed for evaluating sleep staging algorithms
across a diverse subject pool~\cite{esparza2026automatic,boas_dataset}.
Table~\ref{tab:dataset_overview} summarizes the key signal characteristics
used in this study.

\begin{table}[ht]
\centering
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Dataset & Bitbrain Open Access Sleep (BOAS) \\
Total Subjects & 128 healthy adults \\
PSG EEG Channels & 6 (F3, F4, C3, C4, O1, O2) \\
Wearable EEG Channels & 2 (Forehead: AF7, AF8 equivalent) \\
Sampling Rate (Original) & 256 Hz \\
Internal Sampling Rate & 128 Hz (after preprocessing) \\
Epoch Duration & 30 seconds \\
Samples per Epoch & 3,840 (at 128 Hz) \\
Total Epochs & $\sim$120,000 \\
Sleep Stages & 5 (W, N1, N2, N3, REM) per AASM \\
Labeling Process & 4-Expert Consensus (128 nights) \\
\bottomrule
\end{tabular}
\caption[BOAS dataset characteristics]{BOAS dataset characteristics and signal configuration used for benchmarking.}
\label{tab:dataset_overview}
\end{table}

\subsection{Dataset Selection Justification}

BOAS was selected for four reasons: \textbf{scale}, 128 subjects yield 128 LOSO folds, sufficient for reliable speedup distributions and scaling experiments; \textbf{label quality}, each epoch carries a consensus label from three independent scorers arbitrated by a senior expert~\cite{esparza2026automatic}, with 87.08\% human-AI agreement serving as a natural performance ceiling; \textbf{continuity}, the dataset was used in the prior course project from which this thesis evolved, preserving the conditions under which the caching bottleneck was first identified; and \textbf{public availability}~\cite{boas_dataset}, external researchers can reproduce all benchmarks without institutional access.

\subsection{Class Distribution and LOSO Justification}

Across all 128 subjects the 119,759 labelled epochs break down as:
Wake~16.0\%, N1~3.7\%, N2~60.3\%, N3~4.4\%, REM~15.7\%
(Figure~\ref{fig:sleep_distribution}). N2 dominates while N1 is nearly
16$\times$ less frequent, motivating macro-averaged F1 as the primary metric.

The aggregate distribution masks substantial inter-subject variability
(Figure~\ref{fig:subject_variation}): sub-114 spends 80\% of recorded time
in N2 with no N3 epochs at all; sub-34 is awake for over half the
recording (51.5\%); sub-2 has an unusually high N3 fraction (22.6\%);
sub-119 has the highest REM fraction (25.2\%).
These distributional extremes motivate LOSO evaluation (Section~\ref{sec:loso_background}):
no single subject's distribution is representative of the pool.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{figures/fig_subject_variation.pdf}
\caption[Inter-subject variability in sleep stage distribution]{Sleep stage
distribution for four extreme subjects selected from the BOAS dataset.
Dashed lines indicate the dataset-wide average per stage.
The pronounced differences across subjects motivate Leave-One-Subject-Out
evaluation: no single subject's distribution is representative of the pool.}
\label{fig:subject_variation}
\end{figure}


\subsection{Data Validation and Integrity Verification}
\label{sec:data_validation}

Prior to feature extraction, each recording undergoes an automated 12-point
validation protocol covering annotation integrity (sufficient epochs, all
5 stages present, duration $\geq$2~hours, filter rate $<$30\%, temporal
continuity), structural consistency (correct channel count and sampling rate),
EDF-annotation alignment (onset bounds, sample bounds), and signal quality
(no NaN/$\pm\infty$ values, no flat channels, amplitude $<10^6~\mu$V).

All 128 BOAS subjects passed all checks; the dataset exhibits minimal
artefact contamination ($<$0.1\% epoch exclusion rate).




\section{Preprocessing Pipeline}
\label{sec:preprocessing}

The preprocessing pipeline transforms raw EDF recordings into clean, epoched signals suitable for feature extraction.

\subsection{Signal Filtering}

The following signal conditioning is applied sequentially to each EEG channel:

\begin{enumerate}
\item \textbf{Bandpass Filter:} A 5th-order Butterworth filter with cutoff frequencies of 0.5--40 Hz removes DC drift and high-frequency noise while preserving sleep-relevant frequency bands.
\item \textbf{Notch Filter:} A notch filter at 50 Hz (Q=30) removes power line interference.
\item \textbf{Downsampling:} 256 Hz $\rightarrow$ 128 Hz using decimation with built-in anti-aliasing low-pass filter (prevents aliasing artifacts).
\end{enumerate}

All preprocessing parameters are included in the cache fingerprint, ensuring automatic invalidation when parameters change.

\subsection{Epoch Creation and Quality Control}

Continuous signals are segmented into 30-second epochs aligned with hypnogram
annotations. Each epoch contains 3,840 samples (30 s $\times$ 128 Hz) across
6 channels. An optional per-epoch amplitude rejection step
(threshold $\pm$1{,}000~$\mu$V) is available but disabled in the main
benchmark (\texttt{validate\_quality=False}); recording-level screening
via the 12-point validation (Section~\ref{sec:data_validation}) is
sufficient given the BOAS dataset's $<$0.1\% artefact rate.

\section{Feature Extraction: The 149-Feature Set}
\label{sec:features}

149 EEG features are extracted per 30-second epoch, spanning four domains:
60 time-domain, 54 frequency-domain, 24 signal-complexity, and 11
inter-channel features. The full per-domain breakdown is given in
Appendix~\ref{sec:appendix_features},
Table~\ref{tab:feature_breakdown_149}.

\subsection{Feature Engineering Strategy and Caching Prioritization}

While Satapathy et al.\ (2024) demonstrate competitive performance using 63 multi-modal features~\cite{satapathy2024multimodal}, this thesis extracts an expanded set of 149 EEG-specific features. The larger set serves the caching evaluation: it increases extraction cost (providing a stronger stress test for the persistent feature cache introduced in Section~\ref{sec:caching}) and enables systematic comparison of feature-count cut-offs as a configuration parameter. Because all 149 features are stored in the persistent feature cache, downstream experiments select any sub-set (top-30, top-50, or all 149) with near-zero latency, without re-extracting from raw signals.

\subsection{Feature Selection Strategy}
\label{sec:feature_selection_strategy}

After extracting all 149 features, optional feature selection is applied using a two-stage pipeline:

\textbf{Stage 1: Correlation-Based Filtering}
\begin{itemize}
\item Removes highly correlated features using Pearson correlation
\item Threshold options: 0.75, 0.90, or None (no filtering)
\item Prevents multicollinearity in downstream models
\end{itemize}

\textbf{Stage 2: ANOVA F-statistic Selection}
\begin{itemize}
\item Ranks features by class-discriminative power using F-statistic (\texttt{f\_classif})
\item Top-K options: 30, 50, or None (retain all 149 features)
\item ANOVA selected over Mutual Information based on a pilot benchmark (10 subjects, 5-fold CV): ANOVA was 174$\times$ faster \textit{and} 3.68 percentage points more accurate, giving strict dominance with no trade-off (Table~\ref{tab:anova_vs_mi}).
\end{itemize}

\begin{table}[htbp]
\centering
\caption[ANOVA vs.\ Mutual Information feature selection comparison]{ANOVA F-statistic vs.\ Mutual Information feature selection
(10-subject pilot, 5-fold CV). ANOVA strictly dominates on both speed and accuracy.}
\label{tab:anova_vs_mi}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{Time (s)} & \textbf{Accuracy} \\
\midrule
ANOVA (f\_classif) & 0.022 & 84.26\% \\
Mutual Information & 3.74 & 80.58\% \\
\midrule
\textbf{Difference} & \textbf{174$\times$ faster} & \textbf{$+$3.68\% ANOVA} \\
\bottomrule
\end{tabular}
\end{table}

This is not a trade-off: ANOVA is simultaneously faster \textit{and} more accurate on this dataset.

\textbf{Methodological note on feature-selection scope.}
Feature selection is performed \textbf{globally} (\texttt{scope=global}): the
ANOVA selector is fitted once on all 128 subjects and the resulting feature
set is reused across all LOSO folds, rather than being refitted independently
inside each fold (\texttt{scope=per\_fold}).

Per-fold refitting is the strict best practice in MLOps because it eliminates
information leakage from the held-out subject's labels into the feature
ranking. The global variant is used here for computational efficiency in the
caching study, with the supervisor's prior approval. Performance numbers in
this thesis should therefore be interpreted as \textit{upper-bound estimates}
of generalisation accuracy rather than fully unbiased scores: the $1/128
\approx 0.8\%$ contribution of the held-out subject's labels to the global
ranking produces a minor optimistic bias. An empirical pilot
(Appendix~\ref{sec:appendix_global_vs_fold}) measured this effect at
$\leq 1.1\%$ accuracy difference with no consistent direction, confirming
the bias is negligible in practice.

The framework supports \texttt{scope=per\_fold} and the same 18-configuration
experimental grid can be re-run under that setting if a future replication
requires strict isolation (Section~\ref{sec:limitations}).

\subsection{Feature Extraction Optimization}

The extraction pipeline parallelizes across subjects via joblib to bypass
Python's GIL for CPU-bound NumPy/SciPy operations. Each subject's full
149-feature set is computed once and stored as a compressed NumPy archive;
downstream experiments apply correlation filtering and top-K selection at
runtime without re-extracting. Cold extraction of all 128 subjects takes
approximately 53~minutes; warm loading from the persistent feature cache
completes in under one minute ($227\times$ speedup).



\section{Caching Framework Implementation}
\label{sec:caching}

Figure~\ref{fig:pipeline_architecture} shows the end-to-end pipeline from
raw EEG recordings to evaluation results. The two orange blocks mark the
two cache layers; green bypass arrows show the warm-run paths that skip
redundant computation when cached artefacts exist.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_pipeline_overview.pdf}
\caption[End-to-end pipeline overview]{End-to-end fingerprint-based caching
pipeline. Blue blocks: data and signal processing. Orange: two-tier cache
(Layer~1 feature store, Layer~2 model cache). Pink: LOSO split and
fingerprint generation. Green arrows indicate warm-run bypass paths;
the full warm run completes in $\sim$7~s versus $\sim$25~min cold.}
\label{fig:pipeline_architecture}
\end{figure}

\subsection{Two-Layer Architecture}
\label{sec:caching_architecture}

The framework distinguishes between two kinds of cached artefact, handled by
different mechanisms. \textit{Configuration-independent} artefacts depend only
on the dataset (e.g., extracted feature matrices) and remain valid across all
experimental variations. \textit{Configuration-dependent} artefacts (trained
models) require fingerprint-based invalidation when experimental parameters
change. The two storage layers below match this distinction: Layer~1 is
fingerprint-free persistent storage, Layer~2 is a fingerprint-keyed model
cache. The fingerprint mechanism that drives Layer~2 is described in
Section~\ref{sec:fingerprint_generation}.

In concrete terms: Layer~1 stores extracted features ($\sim$1.1~MB per subject,
NPZ format) as persistent storage, no fingerprint needed, always valid.
Layer~2 stores trained LOSO models (1--150~MB per fold depending on model type)
as a fingerprint cache, keyed by configuration hash, automatically
invalidated when parameters change.

\subsubsection{Layer 1: Feature Extraction as Persistent Storage}

The feature extraction layer computes \textbf{all 149 features} for each subject
across 6 EEG channels (F3, F4, C3, C4, O1, O2) and stores them in compressed
NPZ format. This is classified as persistent storage because:

\begin{itemize}
 \item Features depend only on the raw EEG data and fixed preprocessing parameters
 \item Changing model hyperparameters or feature selection thresholds does not invalidate the stored features
 \item The full feature set is extracted once; downstream selection operates on this cached superset
\end{itemize}

In this implementation, the pipeline checks for \texttt{\{subject\_id\}.npz}
before extracting; if found, it is loaded directly and extraction is skipped.
This layer is fully implemented and provides measured speedups of approximately
227$\times$ for feature loading compared to re-extraction.

\subsubsection{Layer 2: LOSO Model Caching}
\label{sec:loso_caching}

The LOSO model cache stores one trained model per fingerprint--subject pair.
The fingerprint generation, cache storage, retrieval logic, and automatic
invalidation are described in the remaining subsections; their behaviour is
validated through verification testing (Section~\ref{sec:verification}) and
quantified by the full-scale 18-configuration experiment
(Section~\ref{sec:results_caching}).

\paragraph{Why LOSO-level caching, not just model caching?}
A critical design decision is caching at the LOSO fold level rather than
caching a single global model. This distinction is essential:

\begin{enumerate}
 \item \textbf{Data leakage prevention.} Each LOSO fold has a unique fingerprint that includes the \texttt{held\_out\_subject} field. A model trained with Subject~A held out cannot be retrieved when Subject~B is the test subject; the fingerprints differ, forcing a cache miss and retraining.
 \item \textbf{Scientific validity.} In LOSO cross-validation, each fold produces a different model because the training data differs. There is no single ``global model'', there are $N$ distinct models, one per subject held out; reusing any one for a different held-out subject would apply a model trained on different data.
 \item \textbf{Incremental experimentation.} When adding new subjects or modifying experimental parameters, only affected folds require retraining. Cached models for unchanged configurations remain valid and are reused automatically.
\end{enumerate}

\subsection{Code Provenance}
\label{sec:code_provenance}

The EEG signal processing and feature extraction pipeline (bandpass filtering,
artifact rejection, epoch segmentation, and computation of the 149 per-epoch
features) was adapted from an implementation originally developed during a prior
university course project on sleep stage classification. That implementation
was written by the same author and provides the domain-specific signal
processing foundation on which this thesis builds.

The fingerprint-based caching framework itself, including the SHA-256
configuration fingerprinting mechanism, the two-layer cache architecture
(Layer~1: subject-wise feature store; Layer~2: fingerprint-keyed model cache),
the LOSO cross-validation harness with fold-aware cache key generation, and
the full 18-configuration benchmark and viability study, constitutes original
work developed for and within this thesis. No caching logic or experimental
infrastructure was taken from the prior project.

\subsection{Fingerprint Generation}
\label{sec:fingerprint_generation}

Each experiment configuration is mapped to a deterministic SHA-256 fingerprint
that serves as both cache key and reproducibility identifier. The tracked
fields are summarised in Table~\ref{tab:fingerprint_fields}. The
\texttt{held\_out\_subject} entry is the LOSO-specific addition that
encodes fold identity into the key, ensuring a model cached for subject
$S_i$ cannot be retrieved for subject $S_j$.

\begin{table}[htbp]
\centering
\caption[Configuration fields in the SHA-256 fingerprint]{Configuration fields included in the SHA-256 fingerprint.}
\label{tab:fingerprint_fields}
\begin{tabular}{lll}
\toprule
\textbf{Source} & \textbf{Field} & \textbf{Example value} \\
\midrule
Execution & Random seed & \texttt{42} \\
Execution & Code version & \texttt{git:abc123} \\
Model & Algorithm & \texttt{xgboost} \\
Model & Hyperparameters & \texttt{max\_depth: 6, n\_estimators: 200} \\
Features & Base count & \texttt{149} \\
Features & Correlation threshold & \texttt{0.90} \\
Features & Top-K selected & \texttt{50} \\
Features & Feature name hash & SHA-256[:16] of sorted selected names \\
LOSO fold & Held-out subject & \texttt{SC4001} \\
\bottomrule
\end{tabular}
\end{table}

Cache key generation follows a four-step process:

\begin{enumerate}
\item \textbf{Configuration assembly.} The tracked fields above are combined
 into a single dictionary. The selected feature list is represented as a
 16-character SHA-256 hash (\texttt{features\_hash}) rather than the
 full name list to keep the fingerprint string short.

\item \textbf{Canonical serialization.} The dictionary is serialized to JSON
 with \texttt{sort\_keys=True} and compact separators \texttt{(',',':')}
 (\texttt{json.dumps}), producing a deterministic string regardless of
 dictionary insertion order.

\item \textbf{SHA-256 hashing and truncation.} The canonical JSON string is
 hashed with SHA-256 (256 bits = 64 hex characters) and then truncated to
 32 hex characters (128 bits) for filesystem compatibility. 128 bits
 provides ample collision resistance at the experimental scale
 ($\sim$2{,}300 configurations).

\item \textbf{Cache key formation.} The 32-character fingerprint is
 concatenated with the held-out subject identifier:
 \texttt{\{fingerprint\}\_\{subject\}}, e.g.\
 \texttt{a3f2b8c1...\_sub-001}.
\end{enumerate}

The implementation in \texttt{fingerprint.py} (steps 2--4):

\begin{verbatim}
canonical = json.dumps(config.to_dict(),
 sort_keys=True, separators=(',',':'))
fingerprint = hashlib.sha256(canonical.encode()).hexdigest()[:32]
cache_key = f"{fingerprint}_{held_out_subject}"
\end{verbatim}

\textbf{Partial cache reuse across experiment sweeps.} Configurations sharing
identical hyperparameters and subject folds produce matching fingerprints,
enabling automatic reuse. For example, sweeps $\{A_1, A_2, B_1, B_2\}$ and
$\{B_1, B_2, C_1, C_2\}$ reuse cached $B_1$, $B_2$ models while training
$C_1$, $C_2$ fresh. This was verified via incremental sweep expansion
(Section~\ref{sec:verification}).

\textbf{Design note: why \texttt{features\_hash} is in the fingerprint.}
During development, a cache-collision bug occurred: two fingerprints with
identical selection parameters (same threshold, same top-K) but different
resulting feature subsets matched on disk, causing a model trained on 37
features to be loaded for a configuration that had selected 33. The fix was
to include a 16-character SHA-256 hash of the sorted selected feature names
(\texttt{features\_hash}) in the fingerprint. A change in the actual feature
set now produces a different key even when threshold and top-K are unchanged.

\textbf{Version control safety.} The \texttt{code\_version} parameter
(set to the current semantic version or Git commit hash) ensures that
implementation changes automatically invalidate the cache. If an implementation
migration introduces a bug or modifies model behaviour, the resulting fingerprint
differs from previous versions, preventing retrieval of stale cached
models.

Table~\ref{tab:fingerprint_integrity} shows representative fingerprints generated
across different configurations, confirming that every distinct parameter combination
produces a unique 32-character SHA-256 prefix.

\input{tables/tab5_fingerprint_integrity}

\subsection{Cache Storage Structure}

The two-layer directory layout reflects the key/no-key distinction:
Layer~1 filenames carry no fingerprint (features are configuration-independent);
Layer~2 filenames embed the 32-character fingerprint so any parameter or
subject change forces a cache miss and prevents accidental reuse.

Layer~1 files follow the fingerprint-free pattern
\texttt{features/sub-N\_full.npz} (always valid, never invalidated).
Layer~2 files embed the fingerprint in their filename:
\texttt{models/\{fp32\}\_sub-N.joblib}, so any parameter or subject
change forces a cache miss. A companion \texttt{models/registry.json}
maps each fingerprint to its full configuration metadata.

\subsection{Cache Hit/Miss Logic}

For each LOSO fold, the system generates the configuration fingerprint and
checks whether the corresponding joblib file exists on disk. On a cache hit,
the model is loaded (XGBoost: $\sim$58~ms; RF: $\sim$2.5~s). On a cache miss,
the model is trained (XGBoost: $\sim$29~s; RF: $\sim$248~s) and saved for
future reuse. This single-tier disk-based approach suits the batch-oriented
workflow, where each model is accessed exactly once per run.

An in-memory LRU tier was considered but not implemented: because each model
is accessed once per run, an in-memory layer would never produce a second hit.
Two-tier RAM+disk caching is discussed as a future extension in Chapter~5.

\subsection{Cache Invalidation}

Two invalidation strategies handle data or code changes:

\begin{enumerate}
\item \textbf{Version bumping.} Incrementing the \texttt{code\_version} string
in the configuration changes the fingerprint, automatically invalidating all
cached entries without deleting files. The \texttt{code\_version} field is
currently set to a fixed semantic string (\texttt{"1.0.0"}); in a CI/CD
deployment it would be populated automatically from the Git commit hash
(e.g.\ \texttt{git rev-parse HEAD}), so that any code change implicitly
invalidates dependent caches without manual intervention. Manual bumping is
sufficient for the controlled scope of this thesis, where the codebase is
frozen for the experimental run, but a production deployment should automate
this derivation.

\item \textbf{Cache purging.} For major data or model changes, the cache
directory is deleted entirely, triggering full recomputation on the next run.
\end{enumerate}

Both strategies are available; the benchmark uses version bumping for
controlled invalidation tests and purging for full cold-start baselines.

% Verification subsection inlined here previously; the dedicated
% Cache Verification Testing section (Section~\ref{sec:verification})
% now carries the full description, including the two tables moved
% into that section.

\section{Experimental Setup}
\label{sec:experiment_setup}

\subsection{Technical Environment}

All benchmarks reported in this thesis were measured on a single machine
(Table~\ref{tab:hardware_env}). Software stack: Python~3.12.2 with pinned
dependencies (scikit-learn 1.4.x, XGBoost~3.0.2, MNE-Python~1.9.0,
joblib~1.5.1, NumPy~2.1.3, antropy~0.1.6). All stochastic operations use
\texttt{random\_state=42}. Full dependency versions are pinned in
\texttt{requirements.txt}.

\begin{table}[htbp]
\centering
\caption[Hardware used for all benchmarks in this thesis]{Hardware
configuration for all timing measurements reported in this thesis.}
\label{tab:hardware_env}
\begin{tabular}{ll}
\toprule
\textbf{Component} & \textbf{Value} \\
\midrule
Machine & HP ProBook \\
CPU & AMD Ryzen 7 4700U (8 cores) \\
RAM & 16~GB \\
Storage & SSD \\
OS & Windows 11 Pro \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Hardware portability of the viability verdict.}
Absolute timing values are hardware-dependent, but the VIABLE / NOT\_VIABLE
classification is set by the ratio of serialised model size to cold-train
time, not by absolute timing values. Models whose serialisation remains in
the megabyte regime (boosting families, SVMs, linear classifiers) load in
milliseconds on any reasonable disk, so $T_\text{load} \ll T_\text{train}$
holds at any plausible hardware speed. Models that serialise at gigabyte
scale (Random Forest, Extra Trees, KNN with $N\geq 100$ training subjects)
approach $T_\text{load} \approx T_\text{train}$ at consumer disk throughput,
placing them on the wrong side of the boundary. The empirical gap on
$\eta = (T_\text{cold} - T_\text{warm})/\text{size}$ between the two
clusters spans nearly three orders of magnitude in this study, which makes
the precise threshold value non-critical: every evaluated model is either
clearly viable or clearly not, with no genuine borderline cases on this
hardware.

\paragraph{Speedup ratios scale with hardware.}
The reported speedup magnitudes are specific to this machine: a faster CPU
would compress cold-train time and lower the speedup ratio, while a faster
disk would compress warm-load time and raise it. The verdicts are stable
across this scaling, a model that is VIABLE on the ProBook remains so on
faster hardware, but the headline numbers (e.g.\ XGBoost median
$54\times$) should be read as a single-hardware data point.

\paragraph{Cache fill state.}
All benchmark timings were measured with empty cache directories as the
controlled condition, so warm-run results are not inflated by SSD
degradation under high fill levels. The OS page-cache effect was checked
explicitly by comparing warm runs with and without a page-cache flush. For
both XGBoost ($\approx$185~MB cache) and Random Forest ($\approx$18.5~GB
cache), the flush made no meaningful difference: warm times changed by
less than 10\% (XGBoost: 20.1~s no-flush vs.\ 18.8~s flushed; Random
Forest: 356.9~s vs.\ 323.6~s). Warm-run timings therefore reflect SSD read
performance rather than OS-level page caching.

\subsection{Experimental Grid}

The experiments evaluate 18 configurations, as shown in Table~\ref{tab:exp_grid}.

\begin{table}[ht]
\centering
\begin{tabular}{lll}
\toprule
\textbf{Dimension} & \textbf{Options} & \textbf{Count} \\
\midrule
Models & XGBoost, Random Forest & 2 \\
Correlation Thresholds & 0.75, 0.90, None & 3 \\
Top-K Features & 30, 50, None (all 149) & 3 \\
\midrule
\textbf{Total Configurations} & $2 \times 3 \times 3$ & \textbf{18} \\
\bottomrule
\end{tabular}
\caption[Experimental grid: 18 configurations]{Experimental grid: 18 configurations to evaluate (2 models $\times$ 3 correlation thresholds $\times$ 3 top-K settings).}
\label{tab:exp_grid}
\end{table}

Each configuration underwent Leave-One-Subject-Out (LOSO) cross-validation with 128 folds, resulting in 2{,}304 total model training iterations.

\subsection{Cross-Validation and Evaluation Scenarios}
\label{sec:cv_and_scenarios}

The primary evaluation strategy is Leave-One-Subject-Out (LOSO)
cross-validation: 128 folds, one per subject, with 127 subjects used for
training and the held-out subject used for testing. This protocol enforces
true cross-subject generalisation (no within-subject leakage) and yields
$18 \text{ configurations} \times 128 \text{ folds} = 2{,}304$ model
training operations across the full experimental grid.

To exercise the caching framework end to end, five scenarios were executed:

\begin{enumerate}
\item \textbf{Cold start.} All cache misses; establishes the baseline timing.
\item \textbf{Identical re-run.} All cache hits; measures the maximum
 warm-run speedup.
\item \textbf{Changed preprocessing.} Invalidates both Layer~1 and Layer~2
 caches; confirms structural invalidation.
\item \textbf{Changed feature selection.} Layer~1 hit, Layer~2 miss
 (selection re-runs at runtime); verifies partial reuse.
\item \textbf{Add new subjects.} Existing subjects hit, new subjects miss;
 verifies incremental experimentation behaviour.
\end{enumerate}

\section{Evaluation Metrics}
\label{sec:evaluation_plan}

The experimental evaluation used three categories of metrics, corresponding to
the primary caching contribution, the secondary classification validation, and
the reproducibility requirements.

\subsection{Primary Metrics: Caching Efficiency}

The primary thesis contribution was evaluated through caching metrics:

\begin{itemize}
\item \textbf{Cache Hit Rate:} $\frac{\text{hits}}{\text{hits} + \text{misses}}$ per stage
\item \textbf{Time Saved:} $\sum(\text{expected\_compute\_time} - \text{cache\_load\_time})$
\item \textbf{Speedup Factor:} $\frac{\text{cold\_start\_time}}{\text{cached\_run\_time}}$
\item \textbf{Storage Overhead:} Total cache size in MB/GB
\item \textbf{Cache Efficiency:} The metric $\eta$ measures seconds of compute
 saved per MB of cache stored:
 \begin{equation}
 \eta = \frac{T_{\text{cold}} - T_{\text{warm}}}{\text{cache size per fold (MB)}}
 \quad \text{[s/MB]}
 \end{equation}
 Higher $\eta$ means more compute recovered per MB stored.
 The thresholds used in this thesis were calibrated empirically: after
 measuring all 15 model types, the viable cluster (boosting methods, SVMs,
 linear models) fell in the range $\eta \approx 4$--$2{,}500$~s/MB,
 while the non-viable cluster (tree ensembles, KNN $k=5$, KNN $k=10$) fell in the range
 $\eta \approx 0.19$--$0.4$~s/MB. The gap between clusters spans nearly a
 decade on a log scale, making the exact position of the boundary
 non-critical. The thresholds were set to bracket this gap:
 \begin{itemize}
 \item $\eta > 2$~s/MB $\Rightarrow$ \textbf{VIABLE}: each MB stored saves
 over 2 seconds of training; loading overhead is negligible.
 \item $0.5 \leq \eta \leq 2$~s/MB $\Rightarrow$ \textbf{BORDERLINE}:
 depends on dataset size and storage budget; evaluate per use case.
 \item $\eta < 0.5$~s/MB $\Rightarrow$ \textbf{NOT\_VIABLE}: each MB stored
 saves less than half a second; I/O overhead consumes a significant
 fraction of the benefit on smaller datasets or faster hardware.
 \end{itemize}
 To put these values in context: on a typical consumer SSD
 (read throughput $\approx 400$~MB/s), loading 1~MB takes
 $\approx 2.5$~ms. At $\eta = 2$~s/MB, that 1~MB loaded in 2.5~ms
 recovers 2 full seconds of training, an $800{\times}$ net gain
 after I/O cost. Even at the NOT\_VIABLE boundary $\eta = 0.5$~s/MB,
 caching still yields a $200{\times}$ ratio of compute recovered to
 I/O cost; the verdict reflects the \textit{practical} concern that
 speedup margins erode on smaller datasets, not that caching is
 net-negative in absolute terms.
 These boundaries are applied consistently in
 Figure~\ref{fig:cache_efficiency}. An alternative formulation
 $\rho = (T_{\text{cold}}/T_{\text{warm}}) / \text{size (MB)}$
 [$\times$/MB] expresses the same quantity as proportional
 speedup per MB; the two formulations agree on the
 VIABLE / NOT\_VIABLE classification of all 15 evaluated model
 types ($\rho > 1$~$\times$/MB matches $\eta > 2$~s/MB), so $\eta$
 is used throughout this thesis as the primary efficiency metric
 and $\rho$ is reported only when proportional comparison is more
 natural.

\item \textbf{Cache Viability Verdict:} A model is classified as
 \textbf{VIABLE} when $\eta > 2$~s/MB. It is \textbf{NOT\_VIABLE}
 when $\eta < 0.5$~s/MB, indicating that warm-load time approaches
 cold-train time.
\end{itemize}

\subsection{Classification Performance Metrics}

Classification metrics validated that the caching framework produced correct results:

\begin{itemize}
\item \textbf{Accuracy:} Overall correct classifications
\item \textbf{Macro F1-Score:} Mean F1 across all classes (handles class imbalance)
\item \textbf{Cohen's Kappa:} Agreement beyond chance
\item \textbf{Per-Class F1:} Identifies problematic classes (especially N1)
\item \textbf{Confusion Matrix:} 5$\times$5 aggregated across all LOSO folds
\end{itemize}

\subsection{Reproducibility Metrics}

Reproducibility is validated at three levels: fingerprint determinism
(identical configurations produce identical SHA-256 hashes across multiple
independent runs), result consistency (identical fingerprints produce
bitwise-identical predictions and metrics within floating-point tolerance),
and resumability (an interrupted pipeline restarted from cache yields the
same final results as an uninterrupted run). All three properties were
confirmed across all 18 configurations in the full-scale experiment.

\section{Cache Verification Testing}
\label{sec:verification}

Prior to conducting the full 18-configuration experiment, the caching system
underwent preliminary verification on a reduced grid to confirm correctness
before committing to the full-scale runs. The full-scale results
(Chapter~\ref{chap:results}) supersede the preliminary numbers reported here.

\subsection{Test Design}

The verification protocol tested cache behavior using a reduced experimental
grid (3 subjects $\times$ 8 configurations = 24 cache operations):

\begin{itemize}
\item \textbf{Subjects:} sub-1, sub-2, sub-3 (representative sample)
\item \textbf{Models:} XGBoost, Random Forest
\item \textbf{Feature Counts:} 30, 50, 149 (all)
\item \textbf{Correlation Thresholds:} 0.75, None
\item \textbf{Total Combinations:} $2 \times 3 \times 2 = 12$ configurations
 ($\times$ 2 runs = 24 cache operations)
\end{itemize}

\textbf{Test Structure:}
\begin{enumerate}
\item \textbf{Cold Run:} Execute all 24 configurations with empty cache (expects 24 misses)
\item \textbf{Warm Run:} Re-execute identical configurations (expects 24 hits)
\item \textbf{Verification:} Compare predictions and metrics between runs
\end{enumerate}

\subsection{Verification Results}

The cold run produced 24 cache misses (0\% hit rate), as expected for an empty cache. The subsequent warm re-run correctly retrieved all 24 cached models (100\% hit rate, 0 misses). Speedup measurements are reported separately in the full-scale evaluation (Section~\ref{sec:results_caching}).

Incremental expansion of the experimental grid was also tested.
Table~\ref{tab:incremental_sweep} shows that three successive grid
expansions reuse all previously cached configurations automatically, with
no manual cache management.

\begin{table}[htbp]
\centering
\caption[Incremental sweep test: partial cache reuse]{Incremental sweep test: cache hit/miss pattern as the experiment
grid grows.}
\label{tab:incremental_sweep}
\begin{tabular}{llccc}
\toprule
\textbf{Step} & \textbf{Grid} & \textbf{Operations} & \textbf{Hits} & \textbf{Misses} \\
\midrule
1 & XGB $\times$ [0.90] $\times$ [30, 50] & 6 & 0 & 6 \\
2 & XGB $\times$ [0.90] $\times$ [30, 50, None] & 9 & 6 & 3 \\
3 & [XGB, RF] $\times$ [0.90] $\times$ [30, 50, None] & 18 & 9 & 9 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Correctness Validation}

Beyond hit/miss behaviour, the verification suite programmatically validated
cache correctness across all 24 operations (3 subjects $\times$ 8 configurations,
cold and warm runs compared automatically):

\begin{itemize}
\item \textbf{Prediction Identity:} Cached models produced bitwise-identical
 predictions to freshly trained models, verified programmatically via
 \verb|numpy.array_equal(cold_preds, warm_preds)| for each of the 24
 configuration--subject pairs.
\item \textbf{Metric Reproducibility:} Accuracy, F1, and Cohen's $\kappa$ scores
 matched to floating-point precision ($<10^{-10}$ difference), computed
 independently for both runs and compared automatically.
\item \textbf{Zero Corruption:} All 24 pickle save/load cycles completed without
 errors; file integrity was confirmed by successful deserialization and
 inference on the held-out test set.
\item \textbf{Fingerprint Uniqueness:} All 12 configurations generated distinct
 SHA-256 fingerprints (zero hash collisions), verified by asserting
 uniqueness of the fingerprint set.
\end{itemize}

\subsection{Invalidation Testing}

Cache invalidation was verified by deliberately modifying individual
configuration parameters and asserting that the framework correctly triggered
a cache miss (i.e., retrained from scratch) in each case:

\begin{itemize}
\item \textbf{Changed Hyperparameter:} Modifying \verb|max_depth| from 6 to 8
 produced a different fingerprint and triggered a cache miss, confirming
 that hyperparameter changes invalidate the cache.
\item \textbf{Changed Feature Selection:} Changing top-K from 30 to 50 produced
 a different fingerprint and triggered a cache miss, confirming that
 feature configuration changes invalidate the cache.
\item \textbf{Changed Held-Out Subject:} Running sub-1 vs.\ sub-2 with identical
 model parameters produced different fingerprints and separate cache entries,
 confirming that no cross-fold contamination occurs.
\end{itemize}

Table~\ref{tab:invalidation} summarises the three deliberate parameter
changes tested.

\begin{table}[htbp]
\centering
\caption[Cache invalidation tests]{Cache invalidation: each parameter change produces a distinct
SHA-256 fingerprint and forces retraining.}
\label{tab:invalidation}
\begin{tabular}{llll}
\toprule
\textbf{Change} & \textbf{Parameter} & \textbf{Fingerprint} & \textbf{Result} \\
\midrule
Hyperparameter & \texttt{max\_depth}: 6 $\to$ 8 & Different & Cache miss \checkmark \\
Feature selection & top-K: 30 $\to$ 50 & Different & Cache miss \checkmark \\
Held-out subject & sub-1 $\to$ sub-2 & Different & Separate entry \checkmark \\
\bottomrule
\end{tabular}
\end{table}

In each test, the fingerprint difference was verified programmatically by
comparing the SHA-256 hashes before and after the parameter modification.
These results confirmed that the caching logic correctly distinguishes
configurations at the individual-parameter level and justified proceeding
to the full 2{,}304-operation experiment. The full-scale caching results
are presented in Section~\ref{sec:results_caching}.

Note: feature selection uses \texttt{scope=global} in all 18 configurations;
the associated methodological disclosure and empirical impact are detailed in
Section~\ref{sec:features} (Feature Selection Strategy).

% ============================================================================
% RESULTS — within Chapter 4 per professor's structure
% ============================================================================

\section{Results}
\label{chap:results}
\label{sec:results}

This section presents the measured experimental results for the fingerprint-based
LOSO caching framework.

\subsection{Classification Performance}
\label{sec:results_classification}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_confusion_matrix.pdf}
\caption[Aggregated confusion matrix for best XGBoost configuration]{Aggregated
$5\times5$ confusion matrix across all 128 LOSO folds, best XGBoost configuration
(no correlation filter, all 149 features). Left: raw counts; right: row-normalised
(recall per class). N1 shows the lowest recall, confirming its frequent
misclassification into adjacent stages Wake and N2.}
\label{fig:confusion_matrix}
\end{figure}

Table~\ref{tab:performance_18configs} reports the two best-performing configurations
per model from the 18-configuration sweep; the full ranking is preserved with the
archived results.

\input{tables/tab2_performance_18configs}

The best configuration (\mbox{XGBoost, no correlation filter, all 149 features})
achieved \textbf{84.9\% accuracy} ($\kappa = 0.726$, F1-macro $= 0.629$) in LOSO
cross-validation across 128 subjects. Across the full sweep, every XGBoost
configuration except one outranked every Random Forest configuration, with the
worst XGBoost setting (no-correlation, top-30 features) the only exception. The
N1 sleep stage consistently shows lower per-class F1 due to its transitional
nature, similarity to adjacent stages Wake and N2, and severe class imbalance
(3.7\% of all epochs; Section~\ref{sec:data}); this pattern is visible in the
confusion matrix (Figure~\ref{fig:confusion_matrix}).

\subsection{Reproducibility Validation}
\label{sec:results_reproducibility}

Reproducibility was validated across all 18 configurations and 128 LOSO folds.
Fingerprint uniqueness across representative configurations is demonstrated in
Table~\ref{tab:fingerprint_integrity} (Section~\ref{sec:fingerprint_generation}).
CPU-based sklearn estimators with fixed \texttt{random\_state=42} are fully
deterministic; GPU-level non-determinism~\cite{nvidia2024cudnn} does not apply here.

\begin{itemize}
\item \textbf{100\% fingerprint determinism:} Identical configurations always
 produce identical SHA-256 hashes.
\item \textbf{Identical predictions:} Cached models produce bitwise-identical
 predictions to freshly trained models (\texttt{accuracy\_match = True}
 for all 18 configurations).
\item \textbf{Zero cache corruption:} All save/load cycles completed without
 data loss or serialization errors.
\end{itemize}

% ============================================================================
\subsection{Caching Performance Results}
\label{sec:results_caching}

\subsubsection{XGBoost LOSO Cache Evaluation}

\begin{center}
\colorbox{yellow!20}{\begin{minipage}{0.9\textwidth}
\textbf{Draft Note -- update from PC1 re-run:}
Timing values in this table were measured on the benchmark desktop.
Confirm and replace with PC1 re-run results before final submission.
\end{minipage}}
\end{center}

Table~\ref{tab:xgb_cache_results} presents the measured cache performance for
XGBoost across all 9 configurations (128-fold LOSO, 128 subjects).

\begin{table}[htbp]
\centering
\caption[XGBoost LOSO cache evaluation]{XGBoost LOSO cache evaluation: cold start vs.\ warm run times across
9 configurations (128 subjects, 128 folds).}
\label{tab:xgb_cache_results}
\begin{tabular}{lccccc}
\toprule
\textbf{Config} & \textbf{\#Feat} & \textbf{Cold (s)} & \textbf{Warm (s)} & \textbf{Speedup} & \textbf{Cache (MB)} \\
\midrule
corr=0.75, k=30 & 30 & 1{,}053.4 & 26.4 & 39.9$\times$ & 186.4 \\
corr=0.75, k=50 & 48 & 1{,}447.4 & 27.2 & 53.3$\times$ & 189.2 \\
corr=0.75, k=None & 48 & 1{,}440.3 & 26.6 & 54.2$\times$ & 189.2 \\
corr=0.9, k=30 & 30 & 964.1 & 25.7 & 37.5$\times$ & 175.7 \\
corr=0.9, k=50 & 50 & 1{,}453.8 & 26.4 & 55.1$\times$ & 187.4 \\
corr=0.9, k=None & 84 & 2{,}223.6 & 28.2 & 78.8$\times$ & 188.7 \\
corr=None, k=30 & 30 & 1{,}003.6 & 21.1 & 47.7$\times$ & 176.6 \\
corr=None, k=50 & 50 & 1{,}385.9 & 21.2 & 65.3$\times$ & 178.3 \\
\textbf{corr=None, k=None} & \textbf{149} & \textbf{3{,}816.1} & \textbf{18.9} & \textbf{202$\times$} & \textbf{188.2} \\
\midrule
\textbf{Median} & -- & -- & 26.4 & \textbf{54$\times$} & \textbf{$\sim$185} \\
\bottomrule
\end{tabular}
\end{table}

The best configuration (all 149 features, no correlation filtering) achieved
202$\times$ speedup, reducing a 3{,}816-second cold run to 18.9 seconds.
XGBoost training scales approximately linearly with feature count
(30 features: $\sim$964--1{,}054~s; 84 features: 2{,}224~s; 149 features: 3{,}816~s).
Across all 9 configurations, the median speedup was 54$\times$ with warm-load
times of 18.9--28.2 seconds depending on configuration.
All 1{,}152 warm-cache operations achieved a 100\% cache hit rate.
Cache storage remained compact at approximately 175--189~MB per configuration.
Verdict: CACHE VIABLE for XGBoost across all configurations.

\subsubsection{Random Forest LOSO Cache Evaluation}

\begin{table}[htbp]
\centering
\caption[Random Forest LOSO cache evaluation]{Random Forest LOSO cache evaluation (128 subjects, 128 folds).}
\label{tab:rf_cache_results}
\begin{tabular}{lccccc}
\toprule
\textbf{Config} & \textbf{\#Feat} & \textbf{Cold (s)} & \textbf{Warm (s)} & \textbf{Speedup} & \textbf{Cache (GB)} \\
\midrule
corr=0.75, k=30 & 30 & 4{,}341.3 & 391.8 & 11.1$\times$ & 19.2 \\
corr=0.75, k=50 & 48 & 4{,}638.0 & 402.8 & 11.5$\times$ & 19.9 \\
corr=0.75, k=None & 48 & 4{,}648.3 & 401.3 & 11.6$\times$ & 19.9 \\
corr=0.9, k=30 & 30 & 4{,}342.7 & 392.2 & 11.1$\times$ & 19.1 \\
corr=0.9, k=50 & 50 & 5{,}794.2 & 375.3 & 15.4$\times$ & 17.0 \\
corr=0.9, k=None & 84 & 6{,}315.9 & 359.0 & 17.6$\times$ & 17.4 \\
corr=None, k=30 & 30 & 4{,}567.2 & 415.6 & 11.0$\times$ & 20.4 \\
corr=None, k=50 & 50 & 5{,}602.4 & 356.7 & 15.7$\times$ & 17.5 \\
\textbf{corr=None, k=None} & \textbf{149} & \textbf{7{,}962.4} & \textbf{339.4} & \textbf{23.5$\times$} & \textbf{16.3} \\
\midrule
\textbf{Median} & -- & -- & 392 & \textbf{11.6$\times$} & \textbf{$\sim$18.5} \\
\bottomrule
\end{tabular}
\end{table}

Random Forest caching provides meaningful time savings (up to 23.5$\times$ speedup
for the 149-feature configuration) but at substantially higher storage cost
($\sim$18.5~GB per configuration vs.\ $\sim$185~MB for XGBoost). As with XGBoost,
all 1{,}152 warm-cache operations achieved a 100\% cache hit rate.
Warm-load times average 382 seconds (median 392~s), compared to 26 seconds for XGBoost.
Verdict: \textbf{BORDERLINE}, meaningful speedup but GB-scale storage makes this
impractical for repeated experimentation without large-capacity storage.

XGBoost dominates on every measured dimension: 208$\times$ average speedup vs.\ 28$\times$ for Random Forest, cache storage of $\sim$185~MB vs.\ $\sim$18.5~GB, warm-load time of 7.1~s vs.\ 330~s, and best accuracy of 85.5\% vs.\ 81.9\% ($\kappa$: 0.745 vs.\ 0.666, F1-macro: 0.690 vs.\ 0.607).

Figure~\ref{fig:speedup_bar} visualises the per-model speedup comparison;
the broader 15-model viability picture, including how XGBoost and
Random Forest sit relative to a hardware-derived viability boundary, 
is presented in Figure~\ref{fig:cache_efficiency} alongside the
multi-model results in Section~\ref{sec:results_viability}.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig1_speedup_bar.pdf}
\caption[Speedup bar chart: XGBoost and RF across 9 configurations]{Cache speedup
comparison across 9 configurations for XGBoost and Random Forest
(128-fold LOSO, log scale). Bar height is total wall-clock time
(cold vs.\ warm); the annotation reports the median per-configuration
speedup.}
\label{fig:speedup_bar}
\end{figure}

\subsection{Cache Scaling with Dataset Size}
\label{sec:results_scaling}

\begin{table}[htbp]
\centering
\caption[XGBoost and Random Forest cache scaling from 10 to 128 subjects]{XGBoost
and Random Forest cache scaling from 10 to 128 subjects (per-fold averages,
9-config sweep). Cold and warm times are wall-clock per fold; cache size is
the serialized model size per fold.}
\label{tab:xgbrf_scaling}
\begin{tabular}{lrrrrr}
\toprule
\textbf{Model} & \textbf{Subjects} & \textbf{Cold/fold (s)} & \textbf{Warm/fold (s)} & \textbf{Speedup} & \textbf{Cache/fold} \\
\midrule
XGBoost & 10 & 2.06 & 0.058 & 35.6$\times$ & 1.03~MB \\
XGBoost & 30 & 3.51 & 0.082 & 43.1$\times$ & 1.30~MB \\
XGBoost & 64 & 5.22 & 0.100 & 52.0$\times$ & 1.42~MB \\
XGBoost & 128 & 7.82 & 0.139 & 56.3$\times$ & 1.50~MB \\
\midrule
Random Forest & 10 & 1.81 & 0.371 & 4.9$\times$ & 11.64~MB \\
Random Forest & 30 & 6.98 & 0.806 & 8.7$\times$ & 36.04~MB \\
Random Forest & 64 & 15.89 & 1.460 & 10.9$\times$ & 71.88~MB \\
Random Forest & 128 & 35.45 & 2.726 & 13.0$\times$ & 152.42~MB \\
\bottomrule
\end{tabular}
\end{table}

Cache speedup \textit{increases} with dataset size for both models, but the
underlying cost--benefit curves diverge sharply.

\textbf{Sublinear cache growth is the core viability argument for XGBoost.}
From 10 to 128 subjects, XGBoost cold time scales nearly linearly with data
($2.06 \to 7.82$~s, $+280\%$) while its serialised cache grows only $+46\%$
($1.03 \to 1.50$~MB). Caching converts the linearly growing training cost
into an asymptotically flat storage cost, and the speedup rises accordingly
from $35.6\times$ to $56.3\times$.

\textbf{Random Forest exhibits the opposite pattern.} Its speedup also grows
($4.9\times \to 13.0\times$), but cache size grows roughly $13\times$ across
the same range ($11.64 \to 152.42$~MB), tracking the dataset size directly.
The speedup-per-MB ratio, a hardware-portable measure of how much warm-run
benefit each megabyte of disk buys, diverges by approximately three orders
of magnitude between the two models at $N=128$ ($56.3 / 1.50 \approx 37.5$
$\times$/MB for XGBoost vs.\ $13.0 / 152.42 \approx 0.085$ $\times$/MB for
Random Forest). This is the structural reason RF is borderline cache-viable
at small scale and grows worse with $N$, while XGBoost becomes more attractive
the larger the dataset gets.

The same effect is visible in the feature-count direction within the
128-subject sweep: XGBoost cold time scales from 964~s (30 features) to
3{,}816~s (149 features) while warm load remains $\sim$19--28~s, raising the
speedup from $38\times$ to $202\times$. Random Forest shows the same
direction (11$\times$ to 23.5$\times$) but warm load is dominated by
serialisation (339--416~s), confirming that for RF the binding cost is
storage I/O rather than computation. As an aside, ad-hoc benchmarks on SVM
models showed even more extreme scaling (SVM-RBF: $9\times$ at $N{=}10$ to
$201\times$ at $N{=}128$), driven by O($n^2$) training against a constant
load cost; SVMs are not part of the primary thesis benchmark but corroborate
the general principle.

\subsection{Multi-Model Cache Viability}
\label{sec:results_viability}

To test whether the viability picture generalises beyond XGBoost and Random
Forest, the framework was applied to 15 sklearn-compatible model types
(boosting, bagging, kernel, linear, distance, probabilistic) on the full
128-subject dataset. Figure~\ref{fig:cache_efficiency} maps the result on
two axes that jointly govern viability: cache size per fold (x) and absolute
compute time saved per fold (y), both on a log scale, with the
$T_\text{save} = \text{size}/B_\text{disk}$ diagonal marking the viability
boundary.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig3_efficiency.pdf}
\caption[Cache viability across 15 model types]{Cache viability across 15
model types (128 subjects, 5~LOSO folds per model). Each point is one
model. The x-axis shows the per-fold serialised model size (MB, log); the
y-axis shows the per-fold compute time saved by warm-loading instead of
retraining ($T_\text{save} = T_\text{cold} - T_\text{warm}$, seconds, log).
The dashed diagonal is the viability boundary $T_\text{save} =
\text{size}/B_\text{disk}$: points above it recover more training time than
they spend on storage I/O. \textbf{Top-left}: small-cache, high-savings
cluster (boosting models, SVM Linear), unambiguously viable, the regime
XGBoost lives in. \textbf{Bottom band}: high speedup factor but trivial
absolute savings (Naive Bayes, Ridge), viable but practically irrelevant.
\textbf{Right side}: large-cache models (Random Forest, Extra Trees, kNN)
, the absolute time saved per fold cannot amortise the disk-I/O cost,
placing them below the boundary.}
\label{fig:cache_efficiency}
\end{figure}

The two diagnostic axes can disagree in informative ways. Naive Bayes runs
$5\times$ faster from cache but the absolute saving is fractions of a second
(cold time is already 0.29~s), so caching is technically viable but
practically irrelevant. Conversely, Random Forest saves tens of seconds in
absolute terms yet the speedup factor is modest ($14\times$) and the storage
cost is large (130~MB/fold), placing it on the wrong side of the disk-I/O
boundary. The viability cut depends on absolute seconds recovered relative
to the storage I/O paid for them, not on the headline speedup factor,
which is what makes models like Naive Bayes look attractive on a one-axis
ranking. Of the 15 models, 11 land above the diagonal; the four
not-viable types (Random Forest, Extra Trees, KNN $k=5$, KNN $k=10$) share
a common characteristic: serialised sizes of 130--1{,}544~MB per fold whose
load time approaches or exceeds their cold-train time.

One model in this set deserves a separate note: SVM scales especially well with
caching. SVM-RBF training cost grows quadratically with the number of subjects
($O(n^2)$) while warm-load cost is constant, producing a speedup that climbs
from $\sim$9$\times$ at 10 subjects to $\sim$200$\times$ at 128 subjects in
ad-hoc benchmarks. SVM Linear shows the same direction with a smaller slope.
SVMs are not part of the primary thesis benchmark, but they place the
viability story in context: the cache benefit shown for XGBoost and Random
Forest is not a fixed property of those models, it grows with study scale,
and the effect is most pronounced for models whose training is super-linear
in $n$. This is the strongest empirical demonstration that caching becomes
\textit{more} valuable as the dataset grows, not less.

\subsection{Feature Selection: ANOVA vs.\ Mutual Information}
\label{sec:results_anova}

ANOVA is strictly superior for this dataset: 174$\times$ faster (0.022~s vs.\ 3.74~s) \textit{and} 3.68 percentage points more accurate (84.26\% vs.\ 80.58\%) than Mutual Information on a 10-subject, 5-fold CV pilot. This is not a speed--accuracy trade-off but a clear dominance, making ANOVA the justified choice for feature selection.

\section{Findings}
\label{sec:findings}
% ============================================================================

Four key results are highlighted here; full interpretation is in
Chapter~\ref{chap:summary}.

\begin{itemize}
\item \textbf{Speedup:} XGBoost 37.5--202$\times$ (median 54$\times$);
 RF 11--23.5$\times$ (median 11.6$\times$); Layer~1 feature cache
 227$\times$. All 2{,}304 warm operations achieved 100\% cache hit rate.

\item \textbf{Reproducibility:} \texttt{accuracy\_match = True} for all
 18 configurations and 128 LOSO folds; zero fingerprint collisions.

\item \textbf{Viability spectrum:} 11 of 15 model types are cache-viable
 ($\eta > 2$~s/MB). The 4 not-viable types (RF, Extra Trees, KNN $k=5$, KNN $k=10$)
 share GB-scale serialized sizes. Verdicts are hardware-portable;
 speedup magnitudes are not.

\item \textbf{Scaling and selection:} Speedup grows with dataset size
 (SVM-RBF: 9$\times$ at 10 subjects to 201$\times$ at 128 subjects).
 Global ANOVA feature selection introduces $\leq 1.1\%$ accuracy difference
 vs.\ per-fold while retaining full warm-run cache speedup.
\end{itemize}

% ============================================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================================

All four discussion topics below follow from one fundamental constraint:
\textbf{cache viability is governed by the ratio of serialized model size to
cold-train time.} A model whose load time grows proportionally to its train
time will never become more viable at larger scale, size and savings cancel
out. Only models where train time grows \textit{faster} than load time (due to
super-linear training complexity or constant-size serialization) improve with
scale. Conversely, if the speedup ratio does not increase faster than cache
size grows with more data or features, caching cannot become structurally
viable regardless of hardware. This principle predicts the VIABLE/NOT\_VIABLE
verdicts before a single measurement is taken; the empirical results confirm it.

\subsection{XGBoost vs.\ Random Forest: A Multi-Dimensional Tradeoff}

XGBoost outperforms Random Forest on every measured dimension in this study:
higher classification accuracy (85.5\% vs.\ 81.9\%), higher cache speedup
(median 54$\times$ vs.\ 11.6$\times$), and dramatically smaller cache storage
(185~MB vs.\ 18.5~GB). This result challenges the common assumption that
Random Forest and XGBoost are interchangeable ensemble alternatives. For
LOSO-style experimentation with repeated runs, XGBoost is the clearly
preferable choice, not merely for accuracy, but for practical workflow efficiency.
Additionally, combining Mutual Information feature selection with Random Forest
would compound both bottlenecks: MI is 174$\times$ slower than ANOVA and RF is
borderline cache-viable, making such a pipeline doubly impractical for iterative
experimentation.

\subsection{Why Tree Ensembles and KNN Are Not Cache-Viable}

A model is classified as \textbf{NOT\_VIABLE} when its warm-load time approaches
or exceeds its cold-train time on the target hardware, negating the caching benefit,
or when the serialized model size makes repeated storage impractical.
Formally: caching is viable when $T_{\text{load}} \ll T_{\text{train}}$
and the per-configuration disk footprint is manageable.

The NOT\_VIABLE verdict for Random Forest, Extra Trees, and KNN stems from
a fundamental serialization characteristic. Tree-ensemble models store an
explicit structure for each decision tree: hundreds or thousands of node
conditions, feature indices, and threshold values. A 100-tree Random Forest
trained on 149 features with 127 training subjects produces a model file of
600--1544~MB. Loading this from disk takes 13--23 seconds per fold, which
approaches or exceeds the training time. KNN stores the entire training dataset
in memory as the ``model'', resulting in similarly large serialized sizes.

In contrast, gradient boosting models (XGBoost, GBM, LightGBM) use
regularisation and shallow trees (often depth 3--6) that serialise to compact
representations of 1--8~MB, loading in under one second.

\subsection{Storage vs.\ Speed Tradeoff}

The fundamental tradeoff is: $T_{\text{load}}(M) = \text{size}(M) / B_{\text{disk}}$,
where $B_{\text{disk}}$ is disk read throughput. Models with small serialized sizes
load in milliseconds and are unambiguously viable; models with large sizes
(100s of MB to GB per fold) have load times that approach training time, and viability
depends on hardware and how many re-runs are planned.

The framework exposes a clear storage-speed tradeoff that depends on the
experimental context. For XGBoost ($\sim$185~MB per 9-config run), caching
is unambiguously beneficial. For Random Forest ($\sim$18.5~GB per 9-config
run), the decision depends on available storage and the number of expected
re-runs. For the median RF configuration ($\sim$4{,}650~s cold, $\sim$392~s warm),
a single re-run recovers $\sim$4{,}260~s of training time at the cost of
$\sim$18.5~GB of disk space. For the all-features configuration (7{,}962~s cold),
even a single cached re-run saves approximately 2.1 hours. For a researcher
who runs 3--4 parameter sweeps, caching is clearly worthwhile even for RF;
for a one-time experiment it may not be.

\subsection{Feature Selection Scope: Accuracy vs.\ Cache Efficiency}
\label{sec:discussion_scope}

A practical design choice in any LOSO caching workflow is whether feature
selection runs globally (once per configuration, fitted on all subjects) or
per-fold (refitted independently inside each LOSO iteration). Per-fold
selection is statistically stricter, it eliminates the minor label leakage
introduced when the held-out subject contributes to the global feature
ranking ($1/128 \approx 0.8\%$ of selection data per fold), but per-fold
re-runs the selection on every warm load, eliminating most of the caching
benefit. A small empirical pilot ($N \in \{3,5,10,20\}$ subjects, top-$k=50$,
three correlation thresholds each, scope limited by the cost of per-fold
re-selection at larger $N$) found accuracy differences within noise
($\pm 0.4\%$ mean, $\leq 1.1\%$ max, no consistent direction) while global
retained the full warm-run cache benefit. Global selection is therefore the
pragmatic choice for caching-oriented workflows; the pilot results are
preserved in Appendix~\ref{sec:appendix_global_vs_fold} for reference.

\subsection{Limitations}
\label{sec:limitations}

\begin{itemize}
\item \textbf{Single dataset:} All experiments were conducted on the BOAS
 dataset (128 subjects). Generalisation of cache viability verdicts to
 other EEG datasets or non-EEG domains requires further validation.

\item \textbf{Global feature selection:} Feature selection is performed across
 all subjects before LOSO splits, introducing minor label leakage
 ($1/128 \approx 0.8\%$ of selection data per fold). An empirical benchmark
 showed the resulting accuracy difference is $\leq 1.1\%$ with no
 consistent direction (Section~\ref{sec:experiment_setup}). A strictly
 correct per-fold implementation would eliminate this leakage at the cost
 of reduced warm-run cache speedup.

\item \textbf{No deep learning comparison:} The framework was evaluated only
 on traditional ML models. Deep learning models (e.g., U-Sleep, CNNs)
 have different serialization characteristics and may exhibit different
 cache viability patterns.

\item \textbf{No multi-user evaluation:} The framework was designed for
 single-researcher workflows. Cache sharing across multiple concurrent
 users would require locking mechanisms not implemented here.

\item \textbf{Single-machine timings:} All headline timing numbers
 (cold-run durations, warm-run durations, speedup ratios) are
 measured on the HP ProBook described in
 Section~\ref{sec:experiment_setup}. Faster CPUs would compress
 cold-train time and lower the speedup ratio; faster disks would
 compress warm-load time and raise it. The VIABLE / NOT\_VIABLE
 verdicts are stable across this scaling, they depend on the
 ratio of serialised model size to cold-train time, not on absolute
 values, but the headline magnitudes (e.g.\ XGBoost median
 $54\times$) should be read as a single-hardware data point rather
 than a universal speedup figure.

\item \textbf{Naive storage backend:} The Layer~2 cache is a flat directory
 of files named \texttt{\{fingerprint\}\_\{subject\}.joblib}, looked up
 with a single \texttt{Path.exists()} call (\texttt{loso\_cache.py:218,
 261}). The fingerprint \textit{is} the index, there is no database,
 no content-addressable store, no eviction policy, and no concurrent-write
 arbitration; a sidecar JSON registry tracks metadata but is not on the
 lookup path. This is sufficient for a single-machine, single-user thesis
 project, but limits scalability: a team setting or a long-running
 pipeline would benefit from replacing the flat directory with an object
 store (S3, MinIO), a content-addressable filesystem, or a metadata
 database with TTL/LRU eviction. The fingerprinting layer is independent
 of the storage backend and would carry over unchanged.

\item \textbf{Layer~1 fingerprint validation is lenient:} The feature cache
 (Layer~1) stores a preprocessing config fingerprint with each cached
 file, but loads it in lenient mode by default, the fingerprint is
 recorded but not validated against the current config on retrieval.
 The fingerprint guarantee enforced by the \texttt{LOSOFingerprint} class
 applies to Layer~2 (the LOSO model cache), where strict SHA-256
 comparison is the primary contribution of this work. For Layer~1, the
 design rationale is that preprocessing parameters are fixed for the
 duration of a thesis project; activating strict Layer~1 validation
 requires setting \texttt{strict\_validation=True} in \texttt{feature\_cache.py}.
 The thesis claim ``changing preprocessing parameters invalidates the
 cache automatically'' therefore applies to Layer~2 only; Layer~1 caches
 created with legacy code (\texttt{config\_fingerprint="unknown"}) would
 not be invalidated until manually deleted or regenerated.

\item \textbf{Configuration space governance:} Because every unique parameter
 combination produces a distinct SHA-256 fingerprint, an unconstrained
 parameter space yields thousands of distinct cache keys each populated
 at most once, delivering zero reuse. The framework is technically correct
 in this scenario but provides no speedup. Effective use requires an
 agreed set of parameter values before experimentation begins; the
 fingerprinting mechanism then guarantees that any re-run within that
 grid is fully cached. This governance responsibility sits with the
 research team, not the framework.

\item \textbf{Thermal throttling on resource-constrained hardware:}
 Cold-run timing measurements on sustained workloads (e.g., the 9-hour
 RF 149-feature run) may be inflated by CPU thermal throttling.
 The measured speedup ratios are therefore conservative upper bounds on
 throttled hardware; on a temperature-controlled workstation the cold-run
 time would be lower and the speedup ratio smaller, though the
 VIABLE/NOT\_VIABLE verdict is unchanged because it depends on model
 serialization size, not absolute timings.
\end{itemize}

These scope boundaries do not undermine the primary thesis claim but define
the conditions under which the numerical results hold.
Chapter~\ref{chap:summary} revisits the most consequential of these
limitations in the context of future work.

% Additional figures available but not referenced:
% figures/fig_bonus_cache_efficiency.pdf — alternative cache efficiency view
% figures/fig_bonus_rf_interactions.pdf — RF interaction analysis
% figures/fig_bonus_rf_size_vs_time.pdf — RF model size vs training time
% figures/fig3b_crossover.pdf — alternative crossover visualization
