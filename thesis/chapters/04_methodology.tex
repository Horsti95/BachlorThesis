\chapter{Methodology and Results}

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:}
Goal 20 pages currently 31 shorten to at least 25
\end{minipage}}
\end{center}

This chapter follows a quantitative experimental methodology. EEG-based sleep
stage classification serves as the validation vehicle --- a domain where LOSO
cross-validation is standard practice --- rather than as the primary
contribution. The contribution is the fingerprint-based caching framework
itself; sleep staging provides a realistic, high-volume workload
(128 subjects, 18 configurations, 2{,}304 training operations) on which to
demonstrate and measure it. Section~\ref{sec:theoretical} establishes the
theoretical foundations, Section~\ref{sec:data} describes the dataset,
Sections~\ref{sec:preprocessing}--\ref{sec:features} detail the signal
processing and feature extraction pipeline, Section~\ref{sec:caching}
explains the caching implementation, Section~\ref{sec:experiment_setup}
outlines the experimental design, and Section~\ref{sec:evaluation_plan}
defines the evaluation metrics.

\section{Theoretical Framework}
\label{sec:theoretical}

The framework is grounded in three key concepts: configuration fingerprinting, a distinction between persistent storage and intelligent caching, and two orthogonal evaluation dimensions.

\subsection{Configuration Fingerprinting}

Configuration fingerprinting provides the foundation for cache key generation and reproducibility tracking. Each experiment configuration is uniquely identified by a cryptographic hash computed as:

\begin{equation}
\text{fingerprint} = \text{SHA256}(\text{canonical\_json}(\text{config}))[:32]
\end{equation}

The fingerprint uses 32 hexadecimal characters (128 bits), providing strong
collision resistance. Table~\ref{tab:fingerprint_fields} lists the tracked
fields and their role.

\begin{table}[htbp]
\centering
\caption{Configuration fields included in the SHA-256 fingerprint.}
\label{tab:fingerprint_fields}
\begin{tabular}{lll}
\toprule
\textbf{Source} & \textbf{Field} & \textbf{Example value} \\
\midrule
Execution & Random seed       & \texttt{42} \\
Execution & Code version      & \texttt{git:abc123} \\
Model     & Algorithm         & \texttt{xgboost} \\
Model     & Hyperparameters   & \texttt{max\_depth: 6, n\_estimators: 200} \\
Features  & Base count        & \texttt{149} \\
Features  & Correlation threshold & \texttt{0.90} \\
Features  & Top-K selected    & \texttt{50} \\
Features  & Feature name hash & SHA-256[:16] of sorted selected names \\
LOSO fold & Held-out subject  & \texttt{SC4001} \\
\bottomrule
\end{tabular}
\end{table}

The held-out subject field is the critical LOSO-specific addition: it encodes
the fold identity directly into the key, ensuring a model cached for subject
$S_i$ can never be retrieved for subject $S_j$. An example combined cache
key: \texttt{a3f2b8c1...\_SC4001.joblib}.

The fingerprint generation module is fully implemented, providing the \texttt{LOSOFingerprint} class with methods for generating fingerprints from configuration objects or dictionaries. The \texttt{generate\_cache\_key()} function provides a convenient interface that combines the fingerprint with the held-out subject ID (e.g., \texttt{a3f2b8c1...\_SC4001}) for direct use as cache file names.

\subsection{Configuration-Independent vs. Configuration-Dependent Caching}
\label{sec:storage_vs_caching}

A fundamental distinction in this framework is between \textit{configuration-independent caching} (artifacts that depend only on the dataset) and \textit{configuration-dependent caching} (artifacts that require fingerprint-based invalidation when experimental parameters change). While both avoid redundant computation, they serve different purposes:

\textbf{Configuration-Independent Cache} refers to artifacts that are computed once per dataset version and remain valid across all experimental variations. These artifacts do not depend on model hyperparameters, feature selection criteria, or cross-validation strategies. In this framework, the extracted feature matrices constitute configuration-independent caching---they are computed once per subject and reused across all experiments.

\textbf{Configuration-Dependent Cache} refers to artifacts whose validity depends on experimental configuration. These artifacts require fingerprint-based invalidation when parameters change. The LOSO model cache, which stores trained models keyed by configuration fingerprint and held-out subject, exemplifies configuration-dependent caching.

Table~\ref{tab:storage_hierarchy} summarizes this distinction:

\begin{table}[htbp]
\centering
\begin{tabular}{lllll}
\toprule
\textbf{Layer} & \textbf{Artifact} & \textbf{Size/fold} & \textbf{Type} & \textbf{Speedup (median)} \\
\midrule
Layer 1 & 149 features (NPZ)     & $\sim$1.1~MB & Persistent Storage  & 227$\times$ \\
Layer 2 & XGBoost model (joblib) & $\sim$1--2~MB  & Fingerprint Cache & 178$\times$ \\
Layer 2 & Random Forest (joblib) & $\sim$120--180~MB & Fingerprint Cache & 17.7$\times$ \\
\bottomrule
\end{tabular}
\caption{Storage hierarchy. Layer~2 cache sizes differ fundamentally by model
family: XGBoost produces compact models (1--2~MB per fold), making it highly
cache-viable; Random Forest produces large serialised trees
(120--180~MB per fold), making it borderline viable depending on available
storage.}
\label{tab:storage_hierarchy}
\end{table}


\subsubsection{Layer 1: Feature Extraction as Persistent Storage}

The feature extraction layer computes \textbf{all 149 features} for each subject across 6 EEG channels (F3, F4, C3, C4, O1, O2) and stores them in compressed NPZ format. This is classified as persistent storage because:

\begin{itemize}
    \item Features depend only on the raw EEG data and fixed preprocessing parameters
    \item Changing model hyperparameters or feature selection thresholds does not invalidate the stored features
    \item The full feature set is extracted once; downstream selection operates on this cached superset
\end{itemize}

In this implementation, the pipeline checks for \texttt{\{subject\_id\}.npz}
before extracting; if found, it is loaded directly and extraction is skipped.
This layer is fully implemented and provides measured speedups of approximately 227$\times$ for feature loading compared to re-extraction.

\subsubsection{Layer 2: LOSO Model Caching}
\label{sec:loso_caching}

The LOSO model cache stores one trained model per fingerprint--subject pair.
The complete system---fingerprint generation, cache storage, retrieval logic,
and automatic invalidation---is validated through verification testing
(Section~\ref{sec:verification}) and the full-scale 18-configuration experiment
(Section~\ref{sec:results_caching}).

\textbf{Full-Scale Results:} The caching system achieved 100\% cache hit rate
across all 18 configurations and 128 LOSO folds (2{,}304 total operations).
Measured speedups for XGBoost ranged from 122$\times$ to 498$\times$ (median
178$\times$) with cache sizes of $\sim$185~MB per 9-configuration sweep
(128 models per configuration, $\sim$1.4~MB per fold). Random Forest achieved
12--110$\times$ speedup (median 17.7$\times$) but required $\sim$18.5~GB per
configuration ($\sim$145~MB per fold).

\textbf{Why LOSO-Level Caching, Not Just Model Caching?}

A critical design decision is caching at the LOSO fold level rather than caching a single global model. This distinction is essential:

\begin{enumerate}
    \item \textbf{Data Leakage Prevention:} Each LOSO fold has a unique fingerprint that includes the \texttt{held\_out\_subject} field. A model trained with Subject~A held out cannot be retrieved when Subject~B is the test subject---the fingerprints differ (as demonstrated by the implemented \texttt{LOSOFingerprint} class), forcing a cache miss and retraining.
    
    \item \textbf{Scientific Validity:} In LOSO cross-validation, each fold produces a different model because the training data differs. There is no single ``global model''---there are $N$ distinct models, one per subject held out; reusing any one for a different held-out subject would apply a model trained on different data.
    
    \item \textbf{Incremental Experimentation:} When adding new subjects or modifying experimental parameters, only affected folds require retraining. Cached models for unchanged configurations remain valid and are reused automatically.
\end{enumerate}

\textbf{Why Cache the Model, Not Just the Metrics?}

Within each LOSO fold, the framework caches the trained model rather than just the evaluation metrics:

\begin{itemize}
    \item \textbf{Post-hoc Analysis:} Cached models enable feature importance analysis, prediction on new data, or computation of additional metrics without retraining.
    \item \textbf{Reproducibility Verification:} Identical fingerprints guarantee identical model states, enabling verification that predictions (not just aggregate metrics) are reproducible.
    \item \textbf{Debugging Capability:} When unexpected results occur, the exact model state can be inspected rather than requiring reproduction of the entire training process.
\end{itemize}

The trade-off is increased storage (models are larger than metric summaries), but this is acceptable given the scientific benefits and modern storage capacities.

\subsection{Two Orthogonal Evaluation Dimensions}

The framework evaluation follows two orthogonal dimensions that structure the experimental analysis:

\begin{itemize}
    \item \textbf{WHY dimension (Optimization Goals):}
    \begin{itemize}
        \item Latency: Time to retrieve cached vs. computed results
        \item Throughput: Subjects processed per unit time
        \item Time Saved: Cumulative computation time avoided across experimental iterations
    \end{itemize}
    \item \textbf{WHERE dimension (Cached Artifact Levels):}
    \begin{itemize}
        \item Layer 1: Extracted feature matrices (persistent storage)
        \item Layer 2: Trained LOSO fold models (fingerprint cache)
        \item Results: Final metrics (always recomputed from cached models for verification)
    \end{itemize}
\end{itemize}

This two-dimensional framework enables systematic evaluation of caching benefits across different experimental scenarios, from simple re-runs (maximum cache hits) to parameter sweeps (partial cache hits) to completely new configurations (cache misses establishing new baselines).

\section{Data: BOAS Dataset}

\label{sec:data}

\subsection{Dataset Overview}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_sleep_stage_distribution.pdf}
\caption[Sleep stage distribution across all 128 BOAS subjects]{Sleep stage
epoch distribution across all 128 BOAS subjects (119,759 epochs total).
N2 dominates at 60.3\% and N1 is severely underrepresented at 3.7\%,
reflecting the natural structure of healthy adult sleep.}
\label{fig:sleep_distribution}
\end{figure}


This research utilizes the Bitbrain Open Access Sleep (BOAS) dataset, a
polysomnographic database designed for evaluating sleep staging algorithms
across a diverse subject pool~\cite{esparza2026automatic,boas_dataset}.
Table~\ref{tab:dataset_overview} summarizes the key signal characteristics
used in this study.

\begin{table}[ht]
\centering
\begin{tabular}{|l|l|}
\hline
\textbf{Parameter} & \textbf{Value} \\
\hline
Dataset & Bitbrain Open Access Sleep (BOAS) \\
Total Subjects & 128 healthy adults \\
PSG EEG Channels & 6 (F3, F4, C3, C4, O1, O2) \\
Wearable EEG Channels & 2 (Forehead: AF7, AF8 equivalent) \\
Sampling Rate (Original) & 256 Hz \\
Internal Sampling Rate & 128 Hz (after preprocessing) \\
Epoch Duration & 30 seconds \\
Samples per Epoch & 3,840 (at 128 Hz) \\
Total Epochs & $\sim$120,000 \\
Sleep Stages & 5 (W, N1, N2, N3, REM) per AASM \\
Labeling Process & 4-Expert Consensus (128 nights) \\
\hline
\end{tabular}
\caption{BOAS dataset characteristics and signal configuration used for benchmarking.}
\label{tab:dataset_overview}
\end{table}

\subsection{Dataset Selection Justification}

BOAS was selected for this benchmark for four complementary reasons.

\textbf{Scale for LOSO.} With 128 subjects, BOAS yields 128 LOSO folds---sufficient
to measure cache hit rates, speedup distributions, and scaling behaviour with
statistical reliability. Smaller datasets would compress the scaling experiments
and reduce confidence in the median speedup estimates.

\textbf{Label quality.} BOAS provides a consensus label derived from three independent
scorers and a fourth senior arbitrator~\cite{esparza2026automatic}, reducing the
impact of inter-rater variability on model training. The dataset creators report
87.08\% human-AI agreement on PSG data, providing a natural performance ceiling
for comparison.

\textbf{Continuity.} The BOAS dataset was used in the prior course project from which
this thesis evolved. Retaining the same dataset preserves the experimental conditions
under which the caching bottleneck was first identified, enabling direct comparison
between the uncached and cached workflows that motivated this research.

\textbf{Public availability.} BOAS is publicly accessible~\cite{boas_dataset}, allowing
external researchers to reproduce the benchmarks on identical data without requiring
institutional access or dataset licensing agreements.

\subsection{Class Distribution and LOSO Justification}

Table~\ref{tab:stage_distribution} reports the exact epoch counts across the
full 128-subject dataset. N2 dominates at 60.3\% while N1 is severely
underrepresented at 3.7\%---nearly 16$\times$ less frequent than N2.
This imbalance motivates the use of macro-averaged F1 as the primary
classification metric, giving equal weight to each stage regardless of
frequency.

\begin{table}[htbp]
\centering
\caption{Sleep stage epoch distribution across the full BOAS dataset
(128 subjects, 119,759 epochs).}
\label{tab:stage_distribution}
\begin{tabular}{lrr}
\toprule
\textbf{Sleep Stage} & \textbf{Epochs} & \textbf{Share} \\
\midrule
Wake (W) & 19,137 & 16.0\% \\
N1       &  4,462 &  3.7\% \\
N2       & 72,181 & 60.3\% \\
N3       &  5,225 &  4.4\% \\
REM      & 18,754 & 15.7\% \\
\midrule
\textbf{Total} & \textbf{119,759} & \textbf{100\%} \\
\bottomrule
\end{tabular}
\end{table}

The aggregate distribution masks substantial inter-subject variability.
Figure~\ref{fig:subject_variation} shows four subjects selected as extreme
cases from the full 128-subject pool: sub-114 spends 80\% of recorded time
in N2 with no N3 epochs at all; sub-34 is awake for over half of the
recording (51.5\% Wake); sub-2 has an unusually high N3 fraction (22.6\%);
and sub-119 has the highest REM fraction in the dataset (25.2\%).
A model trained on sub-114 would never observe N3 training examples
and would generalise poorly to sub-2; conversely, a model trained
predominantly on Wake-heavy subjects would be poorly calibrated for
subjects with near-zero Wake epochs.
This variability is precisely why Leave-One-Subject-Out cross-validation
is required: each subject must serve as the held-out test set at least
once, so that evaluation is never contaminated by the subject-specific
distribution the model was trained on.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{figures/fig_subject_variation.pdf}
\caption[Inter-subject variability in sleep stage distribution]{Sleep stage
distribution for four extreme subjects selected from the BOAS dataset.
Dashed lines indicate the dataset-wide average per stage.
The pronounced differences across subjects motivate Leave-One-Subject-Out
evaluation: no single subject's distribution is representative of the pool.}
\label{fig:subject_variation}
\end{figure}


\subsection{Data Validation and Integrity Verification}
\label{sec:data_validation}

Prior to feature extraction, each recording undergoes an automated 12-point
validation protocol covering annotation integrity (sufficient epochs, all
5 stages present, duration $\geq$2~hours, filter rate $<$30\%, temporal
continuity), structural consistency (correct channel count and sampling rate),
EDF-annotation alignment (onset bounds, sample bounds), and signal quality
(no NaN/$\pm\infty$ values, no flat channels, amplitude $<10^6~\mu$V).

All 128 BOAS subjects passed all checks. The dataset exhibits minimal
artifact contamination ($<$0.1\% epoch exclusion rate). The dataset
creators report a human-AI agreement of 87.08\% for PSG
data~\cite{esparza2026automatic}, which serves as a performance benchmark
in this thesis.




\section{Preprocessing Pipeline}
\label{sec:preprocessing}

The preprocessing pipeline transforms raw EDF recordings into clean, epoched signals suitable for feature extraction.

\subsection{Signal Filtering}

The following signal conditioning is applied sequentially to each EEG channel:

\begin{enumerate}
\item \textbf{Bandpass Filter:} A 5th-order Butterworth filter with cutoff frequencies of 0.5--40 Hz removes DC drift and high-frequency noise while preserving sleep-relevant frequency bands.
\item \textbf{Notch Filter:} A notch filter at 50 Hz (Q=30) removes power line interference.
\item \textbf{Downsampling:} 256 Hz $\rightarrow$ 128 Hz using decimation with built-in anti-aliasing low-pass filter (prevents aliasing artifacts).
\end{enumerate}

All preprocessing parameters are included in the cache fingerprint, ensuring automatic invalidation when parameters change.

\subsection{Epoch Creation}

Continuous signals are segmented into 30-second epochs aligned with hypnogram annotations. Each epoch contains 3,840 samples (30 seconds $\times$ 128 Hz) across 6 channels, resulting in an array of shape $(n_\text{epochs}, 3840, 6)$ per subject.

\subsection{Quality Control}

Epochs failing the criteria established in Section~\ref{sec:data_validation}
(amplitude $>\pm500~\mu$V, NaN/infinite values, or invalid annotations) are
excluded before feature extraction.

\section{Feature Extraction: The 149-Feature Set}
\label{sec:features}

149 EEG features are extracted per 30-second epoch, spanning four domains
(Table~\ref{tab:feature_breakdown_149}).

\begin{table}[ht]
\centering
\begin{tabular}{|p{3.5cm}|p{1.5cm}|p{7cm}|}
\hline
\textbf{Domain} & \textbf{Count} & \textbf{Features} \\
\hline
Time-Domain & 60 & Mean, std, variance, min, max, peak-to-peak, RMS, skewness, kurtosis, zero-crossing rate (10 $\times$ 6 channels) \\
\hline
Frequency-Domain & 54 & Band powers (delta 0.5--4 Hz, theta 4--8 Hz, alpha 8--13 Hz, sigma 12--15 Hz, beta 13--30 Hz, gamma 30--40 Hz), spectral entropy, peak frequency, median frequency (9 $\times$ 6 channels) \\
\hline
Signal Complexity & 24 & Hjorth mobility, Hjorth complexity, Hurst exponent, Detrended Fluctuation Analysis (4 $\times$ 6 channels) \\
\hline
Inter-Channel & 11 & Coherence pairs (6), Phase Locking Value pairs (3), global entropy, global complexity \\
\hline
\textbf{Total} & \textbf{149} & \textbf{Comprehensive EEG-based feature set} \\
\hline
\end{tabular}
\caption{Complete 149-feature breakdown for sleep stage classification.}
\label{tab:feature_breakdown_149}
\end{table}

\subsection{Feature Engineering Strategy and Caching Prioritization}

While Satapathy et al.\ (2024) demonstrate competitive performance using 63 multi-modal features~\cite{satapathy2024multimodal}, this thesis extracts an expanded set of 149 EEG-specific features. The larger set serves the caching evaluation: it increases extraction cost (providing a stronger stress test for Layer~1) and enables systematic comparison of feature-count cut-offs as a configuration parameter. Because all 149 features are stored in the persistent Layer~1 cache, downstream experiments select any sub-set (top-30, top-50, or all 149) with near-zero latency, without re-extracting from raw signals.

\subsection{Feature Selection Strategy}

After extracting all 149 features, optional feature selection is applied using a two-stage pipeline:

\textbf{Stage 1: Correlation-Based Filtering}
\begin{itemize}
\item Removes highly correlated features using Pearson correlation
\item Threshold options: 0.75, 0.90, or None (no filtering)
\item Prevents multicollinearity in downstream models
\end{itemize}

\textbf{Stage 2: ANOVA F-statistic Selection}
\begin{itemize}
\item Ranks features by class-discriminative power using F-statistic (\texttt{f\_classif})
\item Top-K options: 30, 50, or None (retain all 149 features)
\item ANOVA selected over Mutual Information based on a pilot benchmark (10 subjects, 5-fold CV): ANOVA was 174$\times$ faster \textit{and} 3.68 percentage points more accurate, giving strict dominance with no trade-off (Table~\ref{tab:anova_vs_mi}).
\end{itemize}

\begin{table}[htbp]
\centering
\caption[ANOVA vs.\ Mutual Information feature selection comparison]{ANOVA F-statistic vs.\ Mutual Information feature selection
(10-subject pilot, 5-fold CV). ANOVA strictly dominates on both speed and accuracy.}
\label{tab:anova_vs_mi}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{Time (s)} & \textbf{Accuracy} \\
\midrule
ANOVA (f\_classif)  & 0.022 & 84.26\% \\
Mutual Information  & 3.74  & 80.58\% \\
\midrule
\textbf{Difference} & \textbf{174$\times$ faster} & \textbf{$+$3.68\% ANOVA} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Implementation Note:} Feature selection is performed \textbf{globally}
(fitted once on all 128 subjects, not independently per fold) for two reasons.
First, a single global fit yields exactly one stable feature set that serves as a
consistent fingerprint component across all LOSO folds, without per-fold
bookkeeping overhead. Second, ANOVA is thereby computed once per configuration
rather than 128 times, reducing feature-selection overhead from $O(N)$ to $O(1)$
relative to fold count.

The trade-off is that the held-out subject's labels contribute to the global
ranking, representing $1/128 \approx 0.8\%$ of the selection data per fold.
This is a known limitation (Section~\ref{sec:limitations}); the empirical impact
and the per-fold speedup advantage of global scope are quantified in the
Discussion (Section~\ref{sec:discussion}).

\subsection{Feature Extraction Optimization}

To enable efficient processing of the BOAS dataset (128 subjects,
$\sim$950--1{,}000 epochs per subject), the extraction pipeline uses
process-based parallelization via joblib to bypass Python's GIL for
CPU-bound NumPy/SciPy operations. Complexity features are accelerated using
the antropy library where applicable---specifically DFA (\texttt{ant.detrended\_fluctuation})
and Hjorth parameters (\texttt{ant.hjorth\_params}); the Hurst exponent retains a
standard R/S-analysis implementation to avoid duplicating the DFA computation.
Each subject's full feature set is computed once and stored as a compressed NumPy
archive; downstream experiments filter to the required channel count and top-K
selection at runtime without re-extracting from raw signals.

Table~\ref{tab:extraction_timing} shows the resulting extraction times for the
full 128-subject dataset. The 227$\times$ Layer~1 cache speedup is measured
against the optimised cold-start baseline; single-threaded environments would
show a larger absolute speedup.

\begin{table}[htbp]
\centering
\caption{Feature extraction timing for the full 128-subject BOAS dataset.}
\label{tab:extraction_timing}
\begin{tabular}{lll}
\toprule
\textbf{Run type} & \textbf{Time (128 subjects)} & \textbf{Note} \\
\midrule
Cold start (parallelised) & $\sim$53 min & Extraction + cache write \\
Warm run (Layer~1 cache)  & $<$1 min     & 227$\times$ speedup \\
\bottomrule
\end{tabular}
\end{table}



\section{Caching Framework Implementation}
\label{sec:caching}

Figure~\ref{fig:pipeline_architecture} shows the end-to-end pipeline from
raw EEG recordings to evaluation results. The two orange blocks mark the
two cache layers; green bypass arrows show the warm-run paths that skip
redundant computation when cached artefacts exist.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_pipeline_overview.pdf}
\caption[End-to-end pipeline overview]{End-to-end fingerprint-based caching
pipeline. Blue blocks: data and signal processing. Orange: two-tier cache
(Layer~1 feature store, Layer~2 model cache). Pink: LOSO split and
fingerprint generation. Green arrows indicate warm-run bypass paths;
the full warm run completes in $\sim$7~s versus $\sim$25~min cold.}
\label{fig:pipeline_architecture}
\end{figure}

\subsection{Code Provenance}
\label{sec:code_provenance}

The EEG signal processing and feature extraction pipeline (bandpass filtering,
artifact rejection, epoch segmentation, and computation of the 149 per-epoch
features) was adapted from an implementation originally developed during a prior
university course project on sleep stage classification. That implementation
was written by the same author and provides the domain-specific signal
processing foundation on which this thesis builds.

The fingerprint-based caching framework itself---including the SHA-256
configuration fingerprinting mechanism, the two-layer cache architecture
(Layer~1: subject-wise feature store; Layer~2: fingerprint-keyed model cache),
the LOSO cross-validation harness with fold-aware cache key generation, and
the full 18-configuration benchmark and viability study---constitutes original
work developed for and within this thesis. No caching logic or experimental
infrastructure was taken from the prior project.

\subsection{Fingerprint Generation}
\label{sec:fingerprint_generation}

Cache key generation follows a five-step process:

\begin{enumerate}
\item \textbf{Configuration assembly.} All tracked parameters are combined
      into a single dictionary: \texttt{random\_seed}, \texttt{code\_version},
      \texttt{model\_config} (algorithm name and hyperparameters),
      \texttt{feature\_config} (base feature count, correlation threshold,
      top-K selection, and a 16-character SHA-256 hash of the sorted selected
      feature names), and \texttt{held\_out\_subject}. The feature list is
      represented as a compact hash (\texttt{features\_hash}) rather than the
      full name list to keep the fingerprint string short.

\item \textbf{Canonical serialization.} The dictionary is serialized to JSON
      with \texttt{sort\_keys=True} and compact separators \texttt{(',',':')}
      (\texttt{json.dumps}), producing a deterministic string regardless of
      dictionary insertion order.

\item \textbf{SHA-256 hashing and truncation.} The canonical JSON string is
      hashed with SHA-256 (256 bits = 64 hex characters) and then truncated to
      32 hex characters (128 bits) for filesystem compatibility. 128 bits
      provides ample collision resistance at the experimental scale
      ($\sim$2{,}300 configurations).

\item \textbf{Cache key formation.} The 32-character fingerprint is
      concatenated with the held-out subject identifier:
      \texttt{\{fingerprint\}\_\{subject\}}, e.g.\
      \texttt{a3f2b8c1...\_sub-001}.
\end{enumerate}

The implementation in \texttt{fingerprint.py} (steps 2--4):

\begin{verbatim}
canonical = json.dumps(config.to_dict(),
                       sort_keys=True, separators=(',',':'))
fingerprint = hashlib.sha256(canonical.encode()).hexdigest()[:32]
cache_key = f"{fingerprint}_{held_out_subject}"
\end{verbatim}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{figures/fig_fingerprint_zoom.pdf}
\caption[Fingerprint generation and cache lookup per LOSO fold]{Detailed
view of fingerprint generation and cache lookup for one LOSO fold.
All experiment parameters (including the held-out subject identifier)
are assembled, serialised to canonical JSON, and hashed with SHA-256.
The resulting 32-character key determines whether a cached model is
loaded (warm path, $\sim$58~ms for XGBoost) or a new model is trained
and stored (cold path).}
\label{fig:fingerprint_zoom}
\end{figure}

\textbf{Partial cache reuse across experiment sweeps.} Configurations sharing
identical hyperparameters and subject folds produce matching fingerprints,
enabling automatic reuse. For example, sweeps $\{A_1, A_2, B_1, B_2\}$ and
$\{B_1, B_2, C_1, C_2\}$ reuse cached $B_1$, $B_2$ models while training
$C_1$, $C_2$ fresh. This was verified via incremental sweep expansion
(Section~\ref{sec:verification}).

\textbf{Design note: why \texttt{features\_hash} is in the fingerprint.}
During development, a cache-collision bug occurred: two fingerprints with
identical selection parameters (same threshold, same top-K) but different
resulting feature subsets matched on disk, causing a model trained on 37
features to be loaded for a configuration that had selected 33. The fix was
to include a 16-character SHA-256 hash of the sorted selected feature names
(\texttt{features\_hash}) in the fingerprint. A change in the actual feature
set now produces a different key even when threshold and top-K are unchanged.

\textbf{Version control safety.} The \texttt{code\_version} parameter
(set to the current semantic version or Git commit hash) ensures that
implementation changes automatically invalidate the cache. If an implementation
migration introduces a bug or modifies model behaviour, the resulting fingerprint
differs from previous versions, preventing retrieval of stale cached
models.

Table~\ref{tab:fingerprint_integrity} shows representative fingerprints generated
across different configurations, confirming that every distinct parameter combination
produces a unique 32-character SHA-256 prefix.

\input{tables/tab5_fingerprint_integrity}

\subsection{Cache Storage Structure}

The two-layer directory layout reflects the key/no-key distinction:
Layer~1 filenames carry no fingerprint (features are configuration-independent);
Layer~2 filenames embed the 32-character fingerprint so any parameter or
subject change forces a cache miss and prevents accidental reuse.

\begin{table}[htbp]
\centering
\caption{Cache directory layout. Layer~1 paths are fingerprint-free (always valid);
Layer~2 paths embed the fingerprint (invalidated on any config change).}
\label{tab:cache_structure}
\begin{tabular}{lp{4cm}p{4.5cm}}
\toprule
\textbf{Layer} & \textbf{Path pattern} & \textbf{Contents} \\
\midrule
1 & \texttt{features/sub-N\_full.npz} & 149 features per subject \\
2 & \texttt{models/\{fp32\}\_sub-N.joblib} & Trained model (invalidated on config change) \\
2 & \texttt{models/registry.json} & Fingerprint $\to$ config metadata index \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Cache Hit/Miss Logic}

For each LOSO fold, the system generates the configuration fingerprint and
checks whether the corresponding joblib file exists on disk. On a cache hit,
the model is loaded (XGBoost: $\sim$58~ms; RF: $\sim$2.5~s). On a cache miss,
the model is trained (XGBoost: $\sim$29~s; RF: $\sim$248~s) and saved for
future reuse. This single-tier disk-based approach suits the batch-oriented
workflow, where each model is accessed exactly once per run.

An in-memory LRU tier was considered but not implemented: because each model
is accessed once per run, an in-memory layer would never produce a second hit.
Two-tier RAM+disk caching is discussed as a future extension in Chapter~5.

\subsection{Cache Invalidation}

Two invalidation strategies handle data or code changes:

\begin{enumerate}
\item \textbf{Version bumping.} Incrementing the \texttt{code\_version} string
in the configuration changes the fingerprint, automatically invalidating all
cached entries without deleting files. The \texttt{code\_version} field is
currently set to a fixed semantic string (\texttt{"1.0.0"}); in a CI/CD
deployment it could be populated automatically from the Git commit hash
(e.g.\ \texttt{git rev-parse HEAD}), ensuring automatic cache invalidation
whenever the codebase changes.

\item \textbf{Cache purging.} For major data or model changes, the cache
directory is deleted entirely, triggering full recomputation on the next run.
\end{enumerate}

Both strategies are available; the benchmark uses version bumping for
controlled invalidation tests and purging for full cold-start baselines.



\section{Experimental Setup}
\label{sec:experiment_setup}

\subsection{Technical Environment}

Two machines were used across the project lifecycle
(Table~\ref{tab:hardware_env}). The HP ProBook laptop was used for initial
pipeline development and unit testing; all timing benchmarks reported in
this thesis were run on the desktop workstation.

\begin{table}[htbp]
\centering
\caption{Hardware used for development and benchmarking.}
\label{tab:hardware_env}
\begin{tabular}{lll}
\toprule
\textbf{Component} & \textbf{Development (HP ProBook)} & \textbf{Benchmark (Desktop)} \\
\midrule
CPU     & AMD Ryzen 7 4700U (8 cores) & Intel Core Ultra 9 275HX (2.70 GHz) \\
RAM     & 16 GB                       & 64 GB \\
Storage & SSD                         & NVMe SSD \\
OS      & Windows 11 Pro              & Windows 11 Pro \\
\bottomrule
\end{tabular}
\end{table}

The software stack is Python~3.12.2 with pinned dependencies (scikit-learn
1.4.x, XGBoost~3.0.2, MNE-Python~1.9.0, joblib~1.5.1, NumPy~2.1.3,
antropy~0.1.6). All stochastic operations use \texttt{random\_state=42}.
Full dependency versions are pinned in \texttt{requirements.txt}.

Absolute timing values are hardware-dependent; however, cache speedup
\textit{ratios} are hardware-portable. Both cold-start training and warm
cache loading scale proportionally with hardware speed: a machine that
trains 3$\times$ faster also reads from an NVMe cache approximately
3$\times$ faster, leaving the speedup ratio approximately constant.
VIABLE/NOT\_VIABLE verdicts are therefore determined by model serialization
size, not by which machine produced the timings
(Section~\ref{sec:results_viability}).
\subsection{Experimental Grid}

The experiments evaluate 18 configurations, as shown in Table~\ref{tab:exp_grid}.

\begin{table}[ht]
\centering
\begin{tabular}{|l|l|l|}
\hline
\textbf{Dimension} & \textbf{Options} & \textbf{Count} \\
\hline
Models & XGBoost, Random Forest & 2 \\
\hline
Correlation Thresholds & 0.75, 0.90, None & 3 \\
\hline
Top-K Features & 30, 50, None (all 149) & 3 \\
\hline
\textbf{Total Configurations} & 2 $\times$ 3 $\times$ 3 & \textbf{18} \\
\hline
\end{tabular}
\caption{Experimental grid: 18 configurations to evaluate.}
\label{tab:exp_grid}
\end{table}

Each configuration underwent Leave-One-Subject-Out (LOSO) cross-validation with 128 folds, resulting in 2{,}304 total model training iterations.

\subsection{Cross-Validation and Evaluation Scenarios}

Each of the 18 configurations is evaluated under 128-fold LOSO
cross-validation: 127 subjects train, 1 is held out for testing, cycling
through all subjects. This produces 2{,}304 individual training operations
and ensures true cross-subject generalization. Five scenarios exercise the
caching behaviour: (1)~cold start (all cache misses, establishes baselines);
(2)~identical re-run (all hits, measures maximum speedup); (3)~changed
preprocessing (invalidates both layers); (4)~changed feature selection
(Layer~1 hit, Layer~2 miss per changed config); (5)~new subjects added
(existing subjects hit, new subjects miss).

\section{Evaluation Metrics}
\label{sec:evaluation_plan}

The experimental evaluation used three categories of metrics, corresponding to
the primary caching contribution, the secondary classification validation, and
the reproducibility requirements.

\subsection{Primary Metrics: Caching Efficiency}

The primary thesis contribution was evaluated through caching metrics:

\begin{itemize}
\item \textbf{Cache Hit Rate:} $\frac{\text{hits}}{\text{hits} + \text{misses}}$ per stage
\item \textbf{Time Saved:} $\sum(\text{expected\_compute\_time} - \text{cache\_load\_time})$
\item \textbf{Speedup Factor:} $\frac{\text{cold\_start\_time}}{\text{cached\_run\_time}}$
\item \textbf{Storage Overhead:} Total cache size in MB/GB
\end{itemize}

\subsection{Secondary Metrics: Classification Performance}

Classification metrics validated that the caching framework produced correct results:

\begin{itemize}
\item \textbf{Accuracy:} Overall correct classifications
\item \textbf{Macro F1-Score:} Mean F1 across all classes (handles class imbalance)
\item \textbf{Cohen's Kappa:} Agreement beyond chance
\item \textbf{Per-Class F1:} Identifies problematic classes (especially N1)
\item \textbf{Confusion Matrix:} 5$\times$5 aggregated across all LOSO folds
\end{itemize}

\subsection{Reproducibility Validation}

Reproducibility is validated at three levels: fingerprint determinism
(identical configurations produce identical SHA-256 hashes across multiple
independent runs), result consistency (identical fingerprints produce
bitwise-identical predictions and metrics within floating-point tolerance),
and resumability (an interrupted pipeline restarted from cache yields the
same final results as an uninterrupted run). All three properties were
confirmed across all 18 configurations in the full-scale experiment.

\section{Cache Implementation Validation}
\label{sec:verification}

Prior to conducting the full 18-configuration experiment, the caching system
underwent preliminary verification on a reduced grid to confirm correctness
before committing to the full-scale runs. The full-scale results
(Section~\ref{sec:results_caching}) supersede the preliminary numbers reported here.

\subsection{Test Design}

The verification protocol used 3 subjects and a reduced grid (12 configurations,
24 cache operations total) to confirm correctness before full-scale runs.
The protocol consists of three tests:

\textbf{Cold/warm correctness:} Empty cache (24 misses) followed by identical
re-run (24 hits); cached and fresh model predictions compared bitwise.

\textbf{Incremental sweep expansion (partial reuse):} Simulates iterative
parameter search by growing the experiment grid in three steps.
Table~\ref{tab:incremental_sweep} shows the expected cache behaviour.

\begin{table}[htbp]
\centering
\caption{Incremental sweep test: cache hit/miss pattern as the experiment
grid grows. Configurations already cached in a prior step are reused;
only new configurations retrain.}
\label{tab:incremental_sweep}
\begin{tabular}{llccc}
\toprule
\textbf{Step} & \textbf{Grid} & \textbf{Operations} & \textbf{Hits} & \textbf{Misses} \\
\midrule
1 & XGB $\times$ [0.90] $\times$ [30, 50] & 6 & 0 & 6 \\
2 & XGB $\times$ [0.90] $\times$ [30, 50, None] & 9 & 6 & 3 \\
3 & [XGB, RF] $\times$ [0.90] $\times$ [30, 50, None] & 18 & 9 & 9 \\
\bottomrule
\end{tabular}
\end{table}

Step~2 adds one top-K value: the two existing configs hit the cache (6 hits),
only the new top-K=None config retrains (3 misses = 1 config $\times$ 3 subjects).
Step~3 adds Random Forest: all 9 XGBoost models hit (already cached), all 9
RF models miss. This demonstrates that the fingerprint correctly distinguishes
configurations within an evolving sweep --- an expanding grid reuses prior
work without any manual cache management.

\textbf{Invalidation correctness:} Verified in Section~\ref{sec:invalidation}.

\subsection{Verification Results}

Table~\ref{tab:cache_verification} summarizes the cache hit/miss behaviour
on the 3-subject verification grid.

\begin{table}[htbp]
\centering
\begin{tabular}{lccc}
\toprule
\textbf{Run} & \textbf{Hits} & \textbf{Misses} & \textbf{Hit Rate} \\
\midrule
Cold Start  & 0  & 24 & 0\%   \\
Warm Re-run & 24 & 0  & 100\% \\
\bottomrule
\end{tabular}
\caption{Cache hit/miss verification on 3 subjects (24 operations).
Cold start correctly produces 24 misses; the subsequent warm re-run
correctly retrieves all 24 cached models. Speedup measurements are
reported separately in the full-scale evaluation
(Section~\ref{sec:results_caching}).}
\label{tab:cache_verification}
\end{table}

\subsection{Correctness Validation}

Beyond hit/miss behaviour, the verification suite programmatically validated
cache correctness across all 24 operations (3 subjects $\times$ 8 configurations,
cold and warm runs compared automatically):

\begin{itemize}
\item \textbf{Prediction Identity:} Cached models produced bitwise-identical
      predictions to freshly trained models, verified programmatically via
      \verb|numpy.array_equal(cold_preds, warm_preds)| for each of the 24
      configuration--subject pairs.
\item \textbf{Metric Reproducibility:} Accuracy, F1, and Cohen's $\kappa$ scores
      matched to floating-point precision ($<10^{-10}$ difference), computed
      independently for both runs and compared automatically.
\item \textbf{Zero Corruption:} All 24 pickle save/load cycles completed without
      errors; file integrity was confirmed by successful deserialization and
      inference on the held-out test set.
\item \textbf{Fingerprint Uniqueness:} All 12 configurations generated distinct
      SHA-256 fingerprints (zero hash collisions), verified by asserting
      uniqueness of the fingerprint set.
\end{itemize}

\subsection{Invalidation Testing}
\label{sec:invalidation}

Table~\ref{tab:invalidation} summarises three deliberate-modification tests,
each verifying that a single parameter change produces a new fingerprint and
a cache miss.

\begin{table}[htbp]
\centering
\caption{Cache invalidation tests: each parameter change produces a distinct
SHA-256 fingerprint and forces retraining.}
\label{tab:invalidation}
\begin{tabular}{llll}
\toprule
\textbf{Change} & \textbf{Parameter} & \textbf{Fingerprint} & \textbf{Result} \\
\midrule
Hyperparameter & \texttt{max\_depth}: 6 $\to$ 8 & Different & Cache miss \checkmark \\
Feature selection & top-K: 30 $\to$ 50 & Different & Cache miss \checkmark \\
Held-out subject & sub-1 $\to$ sub-2 & Different & Separate entry \checkmark \\
\bottomrule
\end{tabular}
\end{table}

All three tests passed programmatically (SHA-256 hashes compared before and
after each modification). The third test confirms no cross-fold contamination:
models for sub-1 and sub-2 are stored under distinct keys and cannot be
retrieved for the wrong subject. These results justified proceeding to the
full 2{,}304-operation experiment
(Section~\ref{sec:results_caching}).

% ============================================================================
% RESULTS — within Chapter 4 per professor's structure
% ============================================================================

\section{Results}
\label{sec:results}

This section presents the measured experimental results for the fingerprint-based
LOSO caching framework.

\subsection{Caching Performance Results}
\label{sec:results_caching}

\subsubsection{XGBoost LOSO Cache Evaluation}

Table~\ref{tab:xgb_cache_results} presents the measured cache performance for
XGBoost across all 9 configurations (128-fold LOSO, 128 subjects).

\begin{table}[htbp]
\centering
\caption{XGBoost LOSO cache evaluation: cold start vs.\ warm run times across
9 configurations (128 subjects, 128 folds).}
\label{tab:xgb_cache_results}
\begin{tabular}{lccccc}
\toprule
\textbf{Config} & \textbf{\#Feat} & \textbf{Cold (s)} & \textbf{Warm (s)} & \textbf{Speedup} & \textbf{Cache (MB)} \\
\midrule
corr=0.75, k=30  & 30  & 905.0   & 6.67 & 135.7$\times$ & 186.4 \\
corr=0.75, k=50  & 48  & 1{,}269.1 & 6.97 & 182.2$\times$ & 189.2 \\
corr=0.75, k=None & 48 & 1{,}280.9 & 7.31 & 175.1$\times$ & 189.2 \\
corr=0.9, k=30   & 30  & 854.0   & 6.95 & 122.9$\times$ & 175.7 \\
corr=0.9, k=50   & 50  & 1{,}317.4 & 7.36 & 179.0$\times$ & 187.4 \\
corr=0.9, k=None & 84  & 2{,}067.0 & 7.49 & 276.1$\times$ & 188.7 \\
corr=None, k=30  & 30  & 918.5   & 7.30 & 125.9$\times$ & 176.6 \\
corr=None, k=50  & 50  & 1{,}277.1 & 7.16 & 178.4$\times$ & 178.3 \\
\textbf{corr=None, k=None} & \textbf{149} & \textbf{3{,}683.3} & \textbf{7.39} & \textbf{498.3$\times$} & \textbf{188.2} \\
\midrule
\textbf{Median} & -- & -- & 7.3 & \textbf{178$\times$} & \textbf{$\sim$185} \\
\bottomrule
\end{tabular}
\end{table}

The best configuration (all 149 features, no correlation filtering) achieved
498$\times$ speedup, reducing a 3{,}683-second cold run to 7.4 seconds.
The 3{,}683-second cold time is not an outlier: XGBoost training scales
approximately linearly with feature count (30 features: $\sim$885~s median;
84 features: 2{,}067~s; 149 features: 3{,}683~s), and 128 sequential LOSO
folds on mobile hardware over $\sim$61 minutes may also include some thermal
throttling, which would inflate the cold-start time and further amplify the
measured speedup. Across all 9 configurations, the median speedup was 178$\times$
with consistent warm-load times around 7 seconds regardless of model complexity.
All 1{,}152 warm-cache operations achieved a 100\% cache hit rate.
Cache storage remained compact at approximately 185--189~MB per configuration.
Verdict: CACHE VIABLE for XGBoost across all configurations.

\subsubsection{Random Forest LOSO Cache Evaluation}

\begin{table}[htbp]
\centering
\caption{Random Forest LOSO cache evaluation (128 subjects, 128 folds).
Config 7 (marked *) may contain an outlier due to process interruption.}
\label{tab:rf_cache_results}
\begin{tabular}{lccccc}
\toprule
\textbf{Config} & \textbf{\#Feat} & \textbf{Cold (s)} & \textbf{Warm (s)} & \textbf{Speedup} & \textbf{Cache (GB)} \\
\midrule
corr=0.75, k=30  & 30  & 4{,}149.4  & 345.7 & 12.0$\times$ & 19.2 \\
corr=0.75, k=50  & 48  & 4{,}401.2  & 355.0 & 12.4$\times$ & 19.9 \\
corr=0.75, k=None & 48 & 4{,}399.1  & 355.4 & 12.4$\times$ & 19.9 \\
corr=0.9, k=30   & 30  & 4{,}126.4  & 351.1 & 11.8$\times$ & 19.1 \\
corr=0.9, k=50   & 50  & 5{,}503.5  & 310.3 & 17.7$\times$ & 17.0 \\
corr=0.9, k=None & 84  & 5{,}971.9  & 309.5 & 19.3$\times$ & 17.4 \\
corr=None, k=30* & 30  & 11{,}331.1 & 356.0 & 31.8$\times$ & 20.4 \\
corr=None, k=50  & 50  & 5{,}465.4  & 305.2 & 17.9$\times$ & 17.5 \\
\textbf{corr=None, k=None} & \textbf{149} & \textbf{31{,}679.5} & \textbf{288.6} & \textbf{109.8$\times$} & \textbf{16.3} \\
\midrule
\textbf{Median} & -- & -- & 346 & \textbf{17.7$\times$} & \textbf{$\sim$18.5} \\
\bottomrule
\end{tabular}
\end{table}

% TODO: re-run or validate — RF corr=None, k=None cold=31,679s may include
% thermal throttling on 9-hour mobile run. RF corr=None, k=30 (11,331s, marked *)
% is anomalous vs. similar configs; likely process interruption during measurement.
Random Forest caching provides significant time savings (up to 110$\times$ speedup)
but at substantially higher storage cost ($\sim$18.5~GB per configuration vs.\ $\sim$185~MB
for XGBoost). As with XGBoost, all 1{,}152 warm-cache operations achieved a 100\% cache hit rate.
Warm-load times average 330 seconds, compared to 7 seconds for XGBoost.
Two cold-run measurements are flagged for re-validation: the 149-feature run
(31{,}679~s, possibly inflated by thermal throttling on a 9-hour mobile run) and
config~7 (corr=None, k=30: 11{,}331~s, marked~* in the table, possibly due to
process interruption). These outliers do not affect the viability verdict.
Verdict: \textbf{BORDERLINE}, meaningful speedup but GB-scale storage makes this
impractical for repeated experimentation without large-capacity storage.

Table~\ref{tab:xgb_rf_comparison} summarises the comparison. XGBoost dominates
on all dimensions: higher accuracy, higher speedup, and dramatically smaller
cache footprint.

\begin{table}[htbp]
\centering
\caption{XGBoost vs.\ Random Forest cache performance summary (medians
across all 9 configurations, 128-fold LOSO).}
\label{tab:xgb_rf_comparison}
\begin{tabular}{lcc}
\toprule
\textbf{Metric} & \textbf{XGBoost} & \textbf{Random Forest} \\
\midrule
Median cache size    & $\sim$185~MB  & $\sim$18.5~GB \\
Median speedup       & 178$\times$   & 17.7$\times$  \\
Median warm time     & 7.3~s         & 346~s         \\
Best accuracy        & 84.9\%        & 81.5\%        \\
Best Cohen's $\kappa$ & 0.726        & 0.647         \\
Best F1-macro        & 0.629         & 0.558         \\
Cache verdict        & \textbf{VIABLE} & \textbf{BORDERLINE} \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:speedup_bar} visualises the speedup comparison, and
Figure~\ref{fig:cache_efficiency} shows cold start vs.\ warm run time
distribution.

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Draft Note -- figure update pending:}
Speedup bar chart needs two fixes before submission:
(1) add ``log scale'' label to the y-axis title;
(2) annotate each bar with its numeric speedup value (especially the small RF bars
which are hard to read on a log scale).
Regenerate after re-run confirms final numbers.
\end{minipage}}
\end{center}
\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig1_speedup_bar.pdf}
\caption{Cache speedup comparison across 9 configurations for XGBoost and
Random Forest (128-fold LOSO, log scale). Numeric speedup values annotated
per bar.}
\label{fig:speedup_bar}
\end{figure}

Figure~\ref{fig:cache_efficiency} visualises the viability spectrum along two
dimensions: how much disk space a cached model requires (x-axis) and how much
compute time it saves per fold (y-axis). The dashed diagonal represents the
\textit{viability boundary} --- the line where disk read time equals training
time saved, i.e.\ $T_{\text{save}} = \text{size} / B_{\text{disk}}$. Models
above the boundary recover more time than they cost in I/O; models below it
do not. XGBoost configurations cluster in the top-left (small cache, large time
saved); RF configurations fall in the right half (large cache, still meaningful
time saved but with much less margin). The boundary is hardware-dependent
(faster disk $\Rightarrow$ boundary shifts left), which is why the absolute
numbers differ across machines while the VIABLE/NOT\_VIABLE rankings do not.

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Draft Note -- re-run pending:}
Cache efficiency scatter may shift once clean re-run completes
(RF cold-run outliers at 11{,}331~s and 31{,}679~s could move RF points).
Regenerate figure from updated benchmark JSON after re-run.
\end{minipage}}
\end{center}
\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig3_efficiency.pdf}
\caption{Cache efficiency scatter: cache size per fold (x-axis, MB, log scale)
vs.\ compute time saved per fold (y-axis, seconds, log scale).
Dashed diagonal = viability boundary ($T_{\text{save}} = \text{size}/B_{\text{disk}}$);
points above the boundary recover more time than they cost in storage I/O.
XGBoost (top-left cluster) is clearly viable; RF (right half) is borderline.}
\label{fig:cache_efficiency}
\end{figure}

\subsection{Classification Performance}
\label{sec:results_classification}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig_confusion_matrix.pdf}
\caption[Aggregated confusion matrix for best XGBoost configuration]{Aggregated
$5\times5$ confusion matrix across all 128 LOSO folds, best XGBoost configuration
(no correlation filter, all 149 features). Left: raw counts; right: row-normalised
(recall per class). N1 shows the lowest recall, confirming its frequent
misclassification into adjacent stages Wake and N2.}
\label{fig:confusion_matrix}
\end{figure}

Table~\ref{tab:performance_18configs} presents the full classification performance
across all 18 experimental configurations, ranked by accuracy.

\input{tables/tab2_performance_18configs}

The best-performing configuration (\mbox{XGBoost, no correlation filter, all 149 features})
achieved \textbf{84.9\% accuracy} ($\kappa = 0.726$, F1-macro $= 0.629$) in LOSO
cross-validation across 128 subjects. XGBoost occupies the top 7 positions in the
ranking, with Random Forest configurations placing 8th--18th. The N1 sleep stage
consistently shows lower per-class F1 due to its transitional nature, similarity
to adjacent stages Wake and N2, and severe class imbalance (3.7\% of all epochs;
Section~\ref{sec:data}). Figure~\ref{fig:per_class_f1} shows the per-class F1 breakdown.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig6_per_class_f1.pdf}
\caption{Per-class F1 scores for best XGBoost configuration (128-fold LOSO).
N1 shows lowest F1 due to transitional stage characteristics.}
\label{fig:per_class_f1}
\end{figure}

\subsection{Reproducibility Results}
\label{sec:results_reproducibility}

Reproducibility was validated across all 18 configurations and 128 LOSO folds.
Fingerprint uniqueness across representative configurations is demonstrated in
Table~\ref{tab:fingerprint_integrity} (Section~\ref{sec:fingerprint_generation}).
CPU-based sklearn estimators with fixed \texttt{random\_state=42} are fully
deterministic; GPU-level non-determinism~\cite{nvidia2024cudnn} does not apply here.

\begin{itemize}
\item \textbf{100\% fingerprint determinism:} Identical configurations always
      produce identical SHA-256 hashes.
\item \textbf{Identical predictions:} Cached models produce bitwise-identical
      predictions to freshly trained models (\texttt{accuracy\_match = True}
      for all 18 configurations).
\item \textbf{Zero cache corruption:} All save/load cycles completed without
      data loss or serialization errors.
\end{itemize}

\noindent\textbf{Extended analyses.}
The primary benchmark (Sections~\ref{sec:results_caching}--\ref{sec:results_reproducibility})
evaluated the framework on XGBoost and Random Forest across 18 configurations and 128 folds.
After confirming the primary results, two secondary studies were conducted to test whether
the viability picture generalises beyond these two models and whether caching benefit
grows with dataset scale. These analyses were not part of the original experimental plan
but emerged naturally from the observation that viability appears to be governed by
serialization size, independent of model family.

\subsection{Multi-Model Cache Viability}
\label{sec:results_viability}

The fingerprint-based caching framework is model-agnostic: any serializable sklearn-compatible estimator is cached identically. Cache viability was assessed across 15 model types spanning boosting, bagging, kernel, and linear families to validate this generalizability.

Table~\ref{tab:cache_viability} summarizes cache viability across 15 model types
tested on the full 128-subject dataset (5 LOSO folds).

\input{tables/tab4_cache_viability}

\textbf{Key finding:} 11 of 15 models are cache-viable. The 4 not-viable model
types (Random Forest, Extra Trees, KNN $k=5$, KNN $k=10$) share a common
characteristic: large serialized model sizes (130--1{,}544~MB per fold) and slow
deserialization times, which eliminate the loading speed advantage over retraining. The top-performing
viable models are boosting-based, with Gradient Boosting achieving 18{,}030$\times$
speedup at only 0.4~MB per fold. Figure~\ref{fig:viability_scatter} visualises
the viability spectrum.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig4b_viability_scatter.pdf}
\caption{Cache viability across 15 model types, ranked by seconds of compute saved per MB of cache (higher = more efficient). Green bars are cache-viable; red bars
are not viable because loading time approaches retraining time.}
\label{fig:viability_scatter}
\end{figure}

\subsection{Cache Scaling with Dataset Size}
\label{sec:results_scaling}

SVM models are used here because their well-characterised training complexity---O($n$) for linear kernels, O($n^2$) for RBF---provides a theoretically grounded test case: as subjects increase, training cost grows predictably while cache loading stays constant, isolating the scaling effect cleanly.

% TODO: add / update data from re-run
Cache speedup increases with dataset size for models whose training cost scales
super-linearly. SVM-RBF illustrates this cleanly: its O($n^2$) training complexity
causes cold run time to grow quadratically with subjects, while warm loading
remains constant, producing a monotonically growing speedup from 9$\times$
(10 subjects) to 201$\times$ (128 subjects). SVM-Linear, being O($n$), shows
approximately stable speedup across scales (1{,}736$\times$ to 3{,}093$\times$
with minor measurement variance at the 30-subject point), confirming that the
cache loading cost remains negligible relative to even linear training costs
at dataset scales above 30 subjects. Figures~\ref{fig:cache_scaling} and~\ref{fig:svm_scaling} visualise
this scaling behaviour.

The same principle holds for the primary models when the feature space grows.
XGBoost training time scales with the number of input features: cold run time
increases from 855 seconds (30 features) to 3{,}683 seconds (149 features), a
4.3$\times$ increase in training cost. Cache loading remains constant at
approximately 7 seconds regardless of feature count, so the speedup grows
from 123$\times$ to 498$\times$ across the same range. Random Forest exhibits
the same directional pattern (12$\times$ to 110$\times$) but with warm-load times
of 288--356 seconds that do not scale with features, reflecting serialization cost
rather than computational cost. This confirms that caching becomes progressively
more valuable as model complexity or feature dimensionality increases---the
framework is not a fixed-speedup tool but one whose benefit grows with the
scale of the experiment.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_bonus_cache_scaling.pdf}
\caption{Cache speedup scaling from 10 to 128 subjects (SVM models).}
\label{fig:cache_scaling}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_bonus_svm_scaling.pdf}
\caption{SVM-Linear and SVM-RBF cache speedup scaling with dataset size.}
\label{fig:svm_scaling}
\end{figure}

% ============================================================================
\section{Findings}
\label{sec:findings}
% ============================================================================

Eight claims formulated at the outset of this thesis were validated through
systematic experimentation:

\begin{enumerate}
\item \textbf{Cache provides significant LOSO speedup.}
      XGBoost: 122--498$\times$ (median 178$\times$).
      SVM-Linear: 1{,}550--3{,}093$\times$ scaling with dataset size.
      The framework delivers order-of-magnitude reductions in experiment time.

\item \textbf{Cached models produce identical results.}
      \texttt{accuracy\_match = True} for all 18 configurations. Fingerprint-based
      cache keys guarantee that retrieved models correspond exactly to the
      requested configuration, ensuring scientific validity.

\item \textbf{ANOVA is strictly superior to Mutual Information for this task.}
      174$\times$ faster with $+$3.68\% higher accuracy. Justified by the
      linear discriminative structure of EEG frequency-domain features.

\item \textbf{Cache benefit scales with dataset size.}
      SVM-RBF speedup grows from 9$\times$ (10 subjects) to 201$\times$
      (128 subjects), because training cost grows quadratically while cache
      loading remains constant. The framework becomes more valuable as studies scale.

\item \textbf{A clear model viability spectrum exists.}
      11 of 15 models are cache-viable. Boosting-based models (GBM, XGBoost,
      AdaBoost) achieve extreme speedups with compact cache sizes. Four model
      types (Random Forest, Extra Trees, KNN $k=5$, KNN $k=10$) are not viable
      due to GB-scale serialized sizes.

\item \textbf{Viability verdicts are hardware-portable.}
      The same models are VIABLE/NOT\_VIABLE regardless of machine. Speedup
      magnitudes differ between the development laptop and the desktop machine,
      but the ranking is consistent, confirming that the framework's behaviour
      generalises across hardware configurations.

\item \textbf{Layer~1 cache provides universally viable speedup.}
      Feature extraction caching (227$\times$ speedup, Layer~1) benefits all
      model types regardless of their Layer~2 viability verdict. Even models
      classified as NOT\_VIABLE at Layer~2 (e.g., Random Forest, KNN) still
      avoid a $\sim$53-minute cold extraction on warm runs, making Layer~1
      caching unambiguously worthwhile for any repeated LOSO experiment.

\item \textbf{Global feature selection achieves equivalent accuracy to per-fold
      selection while enabling significantly better cache reuse.}
      Mean accuracy difference across all configurations and subject counts is
      $\pm 0.4\%$ with no consistent direction --- the leakage from
      $1/128 \approx 0.8\%$ label overlap per fold is empirically negligible.
      In exchange, global scope preserves a stable fingerprint across all folds,
      and warm-run speedup for XGBoost is 3--5$\times$ higher than per-fold
      scope because feature selection does not re-run on cache hits.
      % TODO: add / update data from re-run
\end{enumerate}

% ============================================================================
\section{Discussion}
\label{sec:discussion}
% ============================================================================

All four discussion topics below follow from one fundamental constraint:
\textbf{cache viability is governed by the ratio of serialized model size to
cold-train time.} A model whose load time grows proportionally to its train
time will never become more viable at larger scale --- size and savings cancel
out. Only models where train time grows \textit{faster} than load time (due to
super-linear training complexity or constant-size serialization) improve with
scale. Conversely, if the speedup ratio does not increase faster than cache
size grows with more data or features, caching cannot become structurally
viable regardless of hardware. This principle predicts the VIABLE/NOT\_VIABLE
verdicts before a single measurement is taken; the empirical results confirm it.

\subsection{XGBoost vs.\ Random Forest: A Multi-Dimensional Tradeoff}

XGBoost outperforms Random Forest on every measured dimension in this study:
higher classification accuracy (84.9\% vs.\ 81.5\%), higher cache speedup
(178$\times$ vs.\ 17.7$\times$), and dramatically smaller cache storage
(185~MB vs.\ 18.5~GB). This result challenges the common assumption that
Random Forest and XGBoost are interchangeable ensemble alternatives. For
LOSO-style experimentation with repeated runs, XGBoost is the clearly
preferable choice, not merely for accuracy, but for practical workflow efficiency.
Additionally, combining Mutual Information feature selection with Random Forest
would compound both bottlenecks: MI is 174$\times$ slower than ANOVA and RF is
borderline cache-viable, making such a pipeline doubly impractical for iterative
experimentation.

\subsection{Why Tree Ensembles and KNN Are Not Cache-Viable}

A model is classified as \textbf{NOT\_VIABLE} when its warm-load time approaches
or exceeds its cold-train time on the target hardware, negating the caching benefit,
or when the serialized model size makes repeated storage impractical.
Formally: caching is viable when $T_{\text{load}} \ll T_{\text{train}}$
and the per-configuration disk footprint is manageable.

The NOT\_VIABLE verdict for Random Forest, Extra Trees, and KNN stems from
a fundamental serialization characteristic. Tree-ensemble models store an
explicit structure for each decision tree: hundreds or thousands of node
conditions, feature indices, and threshold values. A 100-tree Random Forest
trained on 149 features with 127 training subjects produces a model file of
600--1544~MB. Loading this from disk takes 13--23 seconds per fold, which
approaches or exceeds the training time. KNN stores the entire training dataset
in memory as the ``model'', resulting in similarly large serialized sizes.

In contrast, gradient boosting models (XGBoost, GBM, LightGBM) use
regularisation and shallow trees (often depth 3--6) that serialise to compact
representations of 1--8~MB, loading in under one second.

\subsection{Storage vs.\ Speed Tradeoff}

The fundamental tradeoff is: $T_{\text{load}}(M) = \text{size}(M) / B_{\text{disk}}$,
where $B_{\text{disk}}$ is disk read throughput. Models with small serialized sizes
load in milliseconds and are unambiguously viable; models with large sizes
(100s of MB to GB per fold) have load times that approach training time, and viability
depends on hardware and how many re-runs are planned.

The framework exposes a clear storage-speed tradeoff that depends on the
experimental context. For XGBoost ($\sim$185~MB per 9-config run), caching
is unambiguously beneficial. For Random Forest ($\sim$18.5~GB per 9-config
run), the decision depends on available storage and the number of expected
re-runs. A single re-run saves approximately 9 hours of computation at the
cost of 18.5~GB of disk space. For a researcher who will run 3--4 parameter
sweeps, this is clearly worthwhile. For a one-time experiment, it may not be.

Figure~\ref{fig:crossover} shows the crossover point at which caching becomes
cost-effective.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig3_crossover.pdf}
\caption{Crossover analysis: number of runs at which caching becomes
cost-effective compared to full recomputation.}
\label{fig:crossover}
\end{figure}

\subsection{Feature Selection Scope: Accuracy vs.\ Cache Efficiency}
\label{sec:discussion_scope}

A practical design choice in any LOSO caching workflow is whether feature
selection runs \textit{globally} (once per configuration, fitted on all subjects)
or \textit{per-fold} (refitted independently inside each LOSO iteration).
Per-fold selection is statistically stricter --- it eliminates the minor
label leakage that arises when the held-out subject's labels contribute to
the global feature ranking --- but it has a significant cache efficiency cost.

Global feature selection (used in this thesis) introduces a structural leakage
of $1/128 \approx 0.8\%$ of selection data per fold. An empirical benchmark
(Table~\ref{tab:global_vs_fold} and Figure~\ref{fig:global_vs_perfold}) measured
this effect directly: the mean accuracy difference across all configurations and
subject counts is $\pm 0.4\%$ with a maximum of $1.1\%$, and the direction is
inconsistent --- per-fold occasionally outperforms global, confirming the effect
is noise rather than systematic bias. The warm-run speedup benefit of global
scope is more significant: XGBoost retains its full 14--23$\times$ warm speedup,
whereas per-fold scope degrades to 4--9$\times$ because feature selection
re-runs for every fold even on cache hits. Global selection is therefore the
pragmatic choice for caching-oriented workflows.

% TODO: add / update data from re-run
\input{tables/tab8_global_vs_fold}

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Draft Note -- figure update pending:}
Global vs.\ per-fold bar chart has overlapping axis labels on the accuracy
difference panel ($+0.006$/$-0.01$ range is too narrow for current tick spacing).
Fix: widen y-axis range, move bar value annotations inside bars or increase
figure height. Regenerate after re-run.
\end{minipage}}
\end{center}
\begin{figure}[h]
\centering
\includegraphics[width=\textwidth]{figures/fig_global_vs_perfold.pdf}
\caption{Global vs.\ per-fold ANOVA: accuracy difference (left) and warm-run
  speedup (right) across subject counts. The accuracy difference is negligible
  ($\leq 1.1\%$, no consistent direction). The warm-run speedup advantage of
  global scope is substantial for XGBoost: at $N=20$ it retains 22.9$\times$
  speedup vs.\ only 4.4$\times$ for per-fold, because per-fold selection must
  re-run once per fold even on cache hits.}
\label{fig:global_vs_perfold}
\end{figure}

\subsection{Limitations}
\label{sec:limitations}

\begin{itemize}
\item \textbf{Single dataset:} All experiments were conducted on the BOAS
      dataset (128 subjects). Generalisation of cache viability verdicts to
      other EEG datasets or non-EEG domains requires further validation.

\item \textbf{Global feature selection:} Feature selection is performed across
      all subjects before LOSO splits, introducing minor label leakage
      ($1/128 \approx 0.8\%$ of selection data per fold). An empirical benchmark
      showed the resulting accuracy difference is $\leq 1.1\%$ with no
      consistent direction (Section~\ref{sec:experiment_setup}). A strictly
      correct per-fold implementation would eliminate this leakage at the cost
      of reduced warm-run cache speedup.

\item \textbf{No deep learning comparison:} The framework was evaluated only
      on traditional ML models. Deep learning models (e.g., U-Sleep, CNNs)
      have different serialization characteristics and may exhibit different
      cache viability patterns.

\item \textbf{No multi-user evaluation:} The framework was designed for
      single-researcher workflows. Cache sharing across multiple concurrent
      users would require locking mechanisms not implemented here.

\item \textbf{Configuration space governance:} Because every unique parameter
      combination produces a distinct SHA-256 fingerprint, an unconstrained
      parameter space yields thousands of distinct cache keys each populated
      at most once, delivering zero reuse. The framework is technically correct
      in this scenario but provides no speedup. Effective use requires an
      agreed set of parameter values before experimentation begins; the
      fingerprinting mechanism then guarantees that any re-run within that
      grid is fully cached. This governance responsibility sits with the
      research team, not the framework.

\item \textbf{Thermal throttling on resource-constrained hardware:}
      Cold-run timing measurements on sustained workloads (e.g., the 9-hour
      RF 149-feature run) may be inflated by CPU thermal throttling.
      The measured speedup ratios are therefore conservative upper bounds on
      throttled hardware; on a temperature-controlled workstation the cold-run
      time would be lower and the speedup ratio smaller, though the
      VIABLE/NOT\_VIABLE verdict is unchanged because it depends on model
      serialization size, not absolute timings.
\end{itemize}

% Additional figures available but not referenced:
% figures/fig_bonus_cache_efficiency.pdf — alternative cache efficiency view
% figures/fig_bonus_rf_interactions.pdf — RF interaction analysis
% figures/fig_bonus_rf_size_vs_time.pdf — RF model size vs training time
% figures/fig3b_crossover.pdf — alternative crossover visualization
