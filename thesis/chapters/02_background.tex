
\chapter{Background}

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 
Lenghth goal 8 pages currently reached  including yellow to does
\end{minipage}}
\end{center}
\section{Domain: Machine Learning Research Methodology}
The landscape of Machine Learning (ML) research and development is
characterized by rapid iteration and experimentation. According to the
AI Index Report 2023, AI-related publications have more than doubled since
2015, reflecting the field's exponential growth~\cite{AIIndex2023}. Data
scientists and ML engineers continuously explore different model architectures,
algorithms, hyperparameters, and data preprocessing techniques to achieve
optimal performance. This iterative process, while essential for innovation,
introduces significant challenges concerning efficiency and reproducibility.
A single experiment may require hours or days of computation, and researchers
often repeat similar computations across multiple runs. The cumulative effect
translates into substantial costs in GPU hours, cloud computing expenses,
and researcher productivity~\cite{schubotz2022caching}.

To manage this complexity, standardized process models have emerged.
CRISP-DM (Cross-Industry Standard Process for Data Mining), developed in
1996, remains widely adopted~\cite{crispdm2000}. However, its limitations
for modern ML---particularly lack of emphasis on model monitoring and
maintenance---led to CRISP-ML(Q), which extends CRISP-DM with quality assurance
methodology tailored for ML applications~\cite{studer2021crispmlq}. This
framework aligns with modern MLOps principles, which emphasize automation,
continuous integration/deployment (CI/CD), and reproducibility throughout
the ML lifecycle~\cite{kreuzberger2023mlops}.

Tools such as MLflow~\cite{zaharia2018mlflow} and MLXP~\cite{arbel2024mlxp} 
support experiment tracking by recording parameters, metrics, code versions, 
and artifacts for each run, enabling comparison and reproducibility. Despite 
these advances, reproducibility remains a pervasive challenge in ML 
research~\cite{semmelrock2025reproducibility}. Reproducibility ensures consistent 
results from an ML workflow under identical conditions, hinging on the 
``Holy Trinity'': input data, code and parameters, and the execution 
environment~\cite{schubotz2022caching}. Challenges arise from:
\begin{itemize}
    \item \textbf{Lack of Records and Hyperparameter Inconsistency:} 
          Unrecorded decisions or parameter changes hinder replication.
    
    \item \textbf{Data and Environment Volatility:} Dataset alterations, 
          framework updates, and inconsistent compute environments cause 
          divergent outcomes. Empirical analysis shows that 61.8\% of 
          CI/CD pipeline modifications in ML projects are driven by 
          such dependency issues~\cite{rzig2024empirical}.

    \item \textbf{Inherent Randomness:} Machine learning algorithms, 
          particularly Feedforward Neural Networks (FFNNs), exhibit 
          significant variation due to stochastic model initialization 
          and training~\cite{raste2022quantifying}. These effects are 
          further amplified by hardware-level non-determinism; for 
          instance, NVIDIA's cuDNN routines utilize atomic operations 
          that introduce "truly random floating point rounding errors" 
          during execution~\cite{nvidia2024cudnn}.
\end{itemize}


These challenges are particularly acute during iterative experimentation,
where researchers may run hundreds of configurations before identifying
promising approaches. Without intelligent management of computational
artifacts, substantial resources are wasted on redundant calculations.
This observation motivates the central contribution of this thesis: a
fingerprint-based caching framework that persists and reuses computational
artifacts (extracted features and trained models) across iterative modeling
phases while maintaining reproducibility through cryptographic configuration
fingerprinting. Specifically, Kreuzberger et al.\ define reproducibility as the ``ability to obtain the exact same results'' (Principle~P3) and highlight metadata tracking for maintaining model lineage (Principle~P7)~\cite{kreuzberger2023mlops}. The fingerprint-based framework implements these principles by hashing a canonical JSON representation of the complete experiment state---including data configuration, code version, model parameters, and held-out subject identifier---into a SHA-256 fingerprint that simultaneously automates lineage tracking and provides a deterministic cache key.

\section{Context: Computational Efficiency in Research}

Building on the iterative experimentation challenges described in
Section 2.1, this section examines the computational efficiency bottlenecks
that motivate intelligent caching frameworks.

The increasing complexity and scale of Machine Learning models have
substantially raised computational demands. Training models, tuning
hyperparameters, and running repeated experiments consume vast compute,
memory, and time resources, creating bottlenecks in research productivity
and driving up costs.

Several factors drive this resource consumption:

\begin{itemize}
\item \textbf{Large Datasets:} Massive datasets require intensive I/O and
      memory usage.

\item \textbf{Complex Models:} Both deep neural networks and ensemble
      methods (e.g., gradient boosting with thousands of trees, large
      random forests) require substantial computational resources for
      training and inference.

\item \textbf{Iterative Experimentation:} Frequent experiments often repeat
      computations unnecessarily when modifying a subset of parameters while
      keeping others constant.

\item \textbf{Hyperparameter Optimization:} Searching large hyperparameter
      spaces is inherently compute-intensive.

\item \textbf{Rigorous Validation:} Cross-validation strategies (e.g.,
      k-fold, leave-one-out) multiply training costs by the number of folds.
\end{itemize}

While caching can help, traditional research in cache management—such as the ML-aided hardware strategies surveyed by Krishna (2025)—focuses primarily on optimizing low-level latency and hardware replacement policies~\cite{krishna2025cache}. These systems are designed to predict microsecond-level access patterns rather than addressing the macro-level requirement of matching high-level experiment configurations to saved artifacts. For EEG research, such low-level caching does not prevent the redundant recomputation of 128-fold cross-validation when hyperparameters change.

This thesis addresses these inefficiencies through a deterministic, fingerprint-based caching framework. By encoding complete experiment configurations (including cross-validation fold identity) into cryptographic SHA-256 cache keys, the framework enables automatic result reuse that is mathematically tied to the scientific parameters, ensuring both efficiency and reproducibility.

\section{Technology: Configuration Fingerprinting and Caching}

Efficient ML experimentation depends on configuration management and intelligent
result reuse. This section reviews technologies directly relevant to the
fingerprint-based LOSO caching approach developed in this thesis.

The adoption of Leave-One-Subject-Out (LOSO) cross-validation is driven by the requirement for algorithmic stability in high-dimensional EEG datasets. As established by Kearns and Ron (1999), leave-one-out estimators provide essential ``sanity-check'' bounds for generalisation error, ensuring that the model does not merely overfit to subject-specific artefacts~\cite{kearns1999}. However, the computational overhead of LOSO remains a significant barrier to iterative research. Jayaram (2018) emphasises that for EEG-based systems to move beyond the laboratory, processing pipelines must be validated across diverse datasets without requiring constant, redundant retraining~\cite{jayaram2018}. In this thesis, LOSO is utilised not only as a validation metric but as a primary driver for the caching framework: the repetitive nature of the 2{,}304 required training runs provides a high-volume workload where deterministic fingerprinting can demonstrate maximum efficiency gains.

\subsection{Configuration Fingerprinting}

Configuration fingerprinting uses cryptographic hash functions (e.g., SHA-256) 
to create unique identifiers for experiment configurations. By hashing a 
canonical representation of all parameters, these fingerprints enable 
deterministic identification of identical configurations and automatic 
cache invalidation when parameters change.

Modern MLOps tools employ variants of this approach:
\begin{itemize}
    \item \textbf{DVC} logs pipeline signatures combining dependency contents, 
          parameters, and commands~\cite{dvc_pipelines}.
    \item \textbf{MLflow} tracks experiment metadata to enable versioning and 
          visual comparison~\cite{zaharia2018mlflow}.
    \item \textbf{MLXP} enforces reproducibility by linking Hydra-based 
          configurations with specific Git commit hashes~\cite{arbel2024mlxp}.
\end{itemize}

\subsection{Hash-Based Caching in MLOps}

MLOps pipeline caching implements content-addressable storage: when pipeline
inputs and code match previous runs (verified through hash comparison),
cached outputs are reused~\cite{dvc_pipelines}. DVC's run cache,
for example, combines dependency hashes with parameter configurations to
create cache keys. When a match is found, execution is skipped and previous
results are restored.

This approach provides reproducibility: identical configurations produce
identical cache keys, ensuring cached results correspond to recorded
parameters~\cite{schubotz2022caching}. When applied to LOSO cross-validation, this approach additionally enables partial pipeline recovery: if a 128-fold experiment is interrupted, the framework can resume from the exact point of failure by loading cached results for all previously completed folds---a capability that remains cumbersome to implement in standard, coarse-grained pipeline tools.
\begin{center}
\colorbox{yellow!20}{% <--- Added opening brace
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note (2.3.2):}
Bezugnahme auf Google's MLOps-Reifegradmodell \cite{googlemlops2023}. Hash-basiertes Caching ist eine Kernkomponente, um von "Level 0" (manueller Prozess) zu einer automatisierten Pipeline zu gelangen. Es dient nicht nur der Effizienz, sondern ist die technische Umsetzung von \textit{Traceability} und \textit{Continuous Training} (CT), da nur durch eindeutige Konfigurations-Fingerprints sichergestellt werden kann, welche Daten- und Code-Versionen zu einem spezifischen Artefakt geführt haben.
\end{minipage}%
}
\end{center}

\subsection{Related Caching Approaches}
\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 

TODO Lennart: Add bib entries for the three systems cited below.
(1)~vLLM/PagedAttention paper (60--80\% memory reduction, 24$\times$ throughput claim).
(2)~GATI learned caching paper (7.69$\times$ latency reduction at 97\% accuracy).
(3)~XRootD/XCache infrastructure report (10.8~PB network savings statistic).
Papers are in your download folder but not yet in references.bib.
\end{minipage}}
\end{center}

While this thesis focuses on hash-based caching for ML experiments, other
caching paradigms exist in adjacent domains:

\textbf{Large Language Model (LLM) Inference Caching} addresses inference
latency by caching attention key-value tensors during autoregressive generation.
Systems like vLLM's PagedAttention reportedly reduce memory waste from 60--80\%
to under 4\% and enable up to 24$\times$ higher throughput.
% TODO Lennart: Add vLLM/PagedAttention bib entry
Prefix caching can achieve up to 85\% latency reduction and 90\% cost savings
for repeated system prompts. These techniques
% TODO Lennart: Add prefix caching bib entry
are specific to LLM inference and not directly applicable to experiment result caching.

\textbf{Learned Caching for Deep Networks} employs neural networks to predict
hidden layer outputs, enabling layer skipping during inference. GATI, for
example, achieves up to 7.69$\times$ latency reduction at 97\% accuracy.
% TODO Lennart: Add GATI bib entry
This represents a different caching
philosophy---using ML to make cache decisions---but applies only to inference scenarios.

\textbf{High-Energy Physics (HEP) Data Caching} operates at petabyte scale
for raw data files. Systems like XRootD/XCache achieve cache hit rates exceeding
85\% by request count. The Southern California Cache infrastructure reportedly
saved 10.8~PB of network traffic in a single year.
% TODO Lennart: Add XRootD/XCache bib entry
However, these systems are optimized for data distribution, not experiment
configuration management.

In contrast to these approaches, this thesis implements deterministic,
hash-based caching specifically designed for cross-validation experiments,
where cache validity depends on both configuration parameters and fold identity.

\subsection{Reproducibility Considerations}

Despite fingerprinting, perfect reproducibility remains challenging. Sources
of non-determinism include random seed variation, software version differences,
hardware-specific floating-point operations, and non-deterministic GPU
algorithms. 

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 
Add and find a cite
\end{minipage}}
\end{center}

% TODO Lennart: Verify the 0.2--0.5% variance statistic. Check Semmelrock (2023),
% Pham et al. 2020, or Bouthillier et al. 2019.

Empirical studies suggest that even with controlled random seeds, small
variance (approximately 0.2--0.5\%) may persist between runs due to hardware-level
floating-point non-determinism. Configuration
% TODO Lennart: Add bib entry for variance statistic
fingerprinting addresses controllable factors (seeds, software versions, data
splits) while accepting that hardware-induced variance cannot be eliminated
through software means. For caching purposes, cached results are considered
valid if configuration fingerprints match, with the understanding that minor
numerical differences may occur due to non-deterministic hardware operations.

% ============================================================================
\section{Sleep Medicine Fundamentals}
\label{sec:sleep_medicine}
% ============================================================================

\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 
Add and find a cite
\begin{itemize}
\item AASM scoring manual: Berry, R.B. et al.\ (2017). \textit{The AASM Manual for
      the Scoring of Sleep and Associated Events}, Version 2.4. American Academy
      of Sleep Medicine.
\item Sleep medicine overview: Carskadon, M.A.\ \& Dement, W.C. (2011).
      ``Normal human sleep: an overview.'' In Kryger, M.H. et al.\ (Eds.),
      \textit{Principles and Practice of Sleep Medicine}, 5th ed., pp.~16--26.
\item A recent review on automated sleep staging (2020--2026).
\end{itemize}
\end{minipage}   }
\end{center}




\subsection{Sleep Stages and Their Characteristics}
\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 
we are doing caching nothing medical should we shorten this?
ideas:
 What to REMOVE (The "Sleep Lab" stuff)The 30-second epoch manual scoring bottleneck: You don't need to describe how long a human takes (250 hours). Just say: "Manual scoring is computationally replaced by automated staging to enable large-scale analysis."Detailed biology of Pyramidal Neurons: Unless you are building a bio-inspired Neural Network, this is "fluff."EOG/EMG details: Since you are only using EEG for your caching logic, don't confuse the reader with sensors you aren't using.2. What to KEEP (The "Data Science" stuff)The 5 Stages (W, N1, N2, N3, REM): You need this because these are your Labels ($y$-variables).The Frequency Bands Table: You need this because these are your Features ($X$-variables). If you change the frequency range in your code, your cache is invalidated. That is the core of your thesis.The 10-20 System (Channels): You need this because F3, C4, etc., are parameters in your Fingerprint.
\end{minipage}   }
\end{center}

Human sleep follows a structured architecture of recurring cycles, each
comprising distinct physiological states identified by characteristic brain
activity patterns. The American Academy of Sleep Medicine (AASM) defines
five sleep stages scored in 30-second epochs:

\begin{itemize}
\item \textbf{Wake (W):} Characterised by low-amplitude, high-frequency EEG
      activity, voluntary eye movements, and sustained muscle tone. Alpha waves
      (8--13~Hz) are prominent when the subject is relaxed with eyes closed.

\item \textbf{N1 (Light Sleep):} The transition from wakefulness. EEG shows
      low-amplitude mixed-frequency activity, with slow rolling eye movements.
      N1 is brief (typically 1--5 minutes) and the most difficult stage to
      classify automatically due to its transitional nature.

\item \textbf{N2 (Sleep):} The most abundant stage, comprising 45--55\% of
      total sleep time. Defined by the presence of sleep spindles (11--16~Hz
      bursts) and K-complexes (large slow waves). Muscle tone decreases and
      eye movements cease.

\item \textbf{N3 (Deep Sleep / Slow-Wave Sleep):} Dominated by high-amplitude
      delta waves (0.5--4~Hz) comprising $\geq$20\% of the epoch. Associated
      with memory consolidation and physical restoration. Most prevalent in
      the first third of the night.

\item \textbf{REM (Rapid Eye Movement):} Characterised by mixed-frequency
      low-amplitude EEG similar to wakefulness, rapid conjugate eye movements,
      and near-complete skeletal muscle atonia (preventing acting out dreams).
      Associated with dreaming and emotional memory processing.
\end{itemize}

A typical adult night consists of 4--6 sleep cycles of approximately 90 minutes
each, with N3 predominating early and REM periods lengthening toward morning.
\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 

idae Combine 
2.4.2 and 2.4.3 as the Focus is cahing not Sleep lab? or shouild we Keep the Details 
The raw input data consists of 6-channel EEG signals recorded via the international 10-20 system. These signals are segmented into 30-second epochs and labeled according to AASM scoring criteria \cite{berry2017aasm}. In the context of the proposed caching architecture, these labels serve as the categorical ground truth, while the physical electrode locations (F3, F4, C3, C4, O1, O2) are treated as immutable metadata in the SHA-256 fingerprint generation.
\end{minipage}   }
\end{center}

\subsection{Electroencephalography Basics}
% TODO Lennart: Consider adding a dedicated EEG textbook cite here
Electroencephalography (EEG) measures electrical potentials generated by
synchronized postsynaptic currents in cortical pyramidal neurons. Scalp
electrodes record the summed activity of millions of neurons, providing
a non-invasive window into brain state.

Electrode placement follows the international 10-20 system, which defines
standardised positions relative to anatomical landmarks. Labels indicate
brain region (F=frontal, C=central, O=occipital, T=temporal, P=parietal)
and hemisphere (odd=left, even=right, z=midline). This thesis uses 6 EEG
channels from the 10-20 system: F3, F4 (frontal), C3, C4 (central), O1, O2
(occipital), providing bilateral coverage of frontal, sensorimotor, and
visual cortices.

\subsection{AASM Scoring and the Need for Automation}
% TODO Lennart: Add AASM manual cite (berry2017aasm already in bib)
Clinical polysomnography (PSG) combines EEG with electro-oculography (EOG)
and electromyography (EMG) for comprehensive sleep staging. A trained sleep
technologist manually scores each 30-second epoch according to AASM criteria.
A full night's recording (typically 7--9 hours) produces 840--1080 epochs,
requiring approximately 1.5--2 hours of expert annotation time per recording.

For research studies with large cohorts, manual scoring becomes a significant
bottleneck. The BOAS dataset (128 subjects) would require over 250 hours of
manual annotation. Automated sleep staging using machine learning directly
addresses this scalability problem. Importantly, inter-rater agreement between
human scorers is typically 80--90\% even among certified experts, providing
a natural performance ceiling for automated systems and a meaningful reference
point for evaluating classifier accuracy.

% ============================================================================
\section{EEG Signal Processing Basics}
\label{sec:eeg_processing}
% ============================================================================

\begin{plotsuggestion}
A \textbf{sample EEG epoch plot} showing raw signals from the 6 channels
(F3, F4, C3, C4, O1, O2) for one representative 30-second epoch per sleep
stage (W, N1, N2, N3, REM) would help readers understand the input data.
MNE-Python can generate this directly from the BOAS dataset.
\end{plotsuggestion}

EEG signals are complex, non-stationary time series that require transformation into the frequency domain to extract physiologically relevant information. The foundational principle of sleep staging relies on the identification of specific oscillatory patterns within these signals \cite{niedermeyer2011}.

\subsection{Frequency Bands}

In this work, EEG signals are decomposed into distinct frequency bands. While specific boundaries vary slightly in literature, this study utilizes the ranges specified in \autoref{tab:eeg_bands}, which align with standard AASM guidelines and recent functional connectivity research in sleep staging \cite{huang2021eeg}.

\begin{table}[htbp]
\centering
\caption[EEG frequency bands used for feature extraction and their sleep-relevant associations]{EEG frequency bands used for feature extraction and their sleep-relevant associations (adapted from \cite{huang2021eeg}).}
\label{tab:eeg_bands}
\begin{tabular}{lll}
\toprule
\textbf{Band} & \textbf{Range (Hz)} & \textbf{Sleep Stage Association} \\
\midrule
Delta    & 0.5--4.0   & Predominant in N3 (Deep sleep); indicates sleep pressure \\
Theta    & 4.0--8.0   & Associated with N1 and REM stages; drowsiness \\
Alpha    & 8.0--13.0  & Hallmarked by relaxed wakefulness; key for Stage W \\
Sigma    & 12.0--16.0 & Sleep spindles; the definitive marker for N2 sleep \\
Beta     & 13.0--30.0 & Active wakefulness; power decreases during NREM sleep \\
Gamma    & 30.0--45.0 & High-frequency processing; typically minimal in sleep \\
\bottomrule
\end{tabular}
\end{table}

Power spectral density (PSD) in these bands, computed via Welch's method, forms the core of the frequency-domain feature extraction pipeline. By quantifying the relative power in these specific ranges, the XGBoost and Random Forest models can distinguish between the synchronized, high-amplitude waves of deep sleep and the desynchronized, low-amplitude patterns of wakefulness.

\subsection{Standard Preprocessing Pipeline}

Raw EEG signals contain multiple sources of contamination that must be
addressed before feature extraction:

\begin{itemize}
\item \textbf{DC offset and slow drift:} Removed by high-pass filtering
      (typically $\geq$0.5~Hz).
\item \textbf{High-frequency noise:} Removed by low-pass filtering
      (typically $\leq$40~Hz for sleep staging).
\item \textbf{Power line interference:} A 50~Hz (Europe) or 60~Hz (USA)
      notch filter removes electrical mains noise.
\item \textbf{Downsampling:} The original 256~Hz sampling rate of the BOAS
      dataset is downsampled to 128~Hz, sufficient for capturing all
      sleep-relevant frequencies ($\leq$40~Hz) while halving computational costs.
\item \textbf{Epoch rejection:} Epochs with amplitudes exceeding $\pm$500~$\mu$V,
      NaN values, or flat channels are excluded as artefacts.
\end{itemize}

These steps constitute the preprocessing pipeline used in this thesis,
producing clean 30-second epochs ready for feature extraction.

% ============================================================================
\section{Machine Learning Ensemble Methods}
\label{sec:ensemble_methods}
% ============================================================================
\begin{center}
\colorbox{yellow!20}{%
\begin{minipage}{0.9\textwidth}
\textbf{Working Draft Note:} 
Add and find a cite
TODO Lennart: Add a machine learning textbook reference for this section.
Suggested: Hastie, T., Tibshirani, R., \& Friedman, J.\ (2009).
\textit{The Elements of Statistical Learning}, 2nd ed., Springer.
(Free PDF available at hastie.su.domains/ElemStatLearn/)
Or: Breiman, L.\ (2001). ``Random Forests.'' \textit{Machine Learning},
45(1), 5--32. (primary source for Random Forest).
\end{minipage}   }
\end{center}





Ensemble methods combine multiple weaker models to produce a stronger
predictor. They are particularly effective for tabular and feature-based
classification tasks, which motivates their use in this thesis for
EEG feature-based sleep staging.

\subsection{Decision Trees}

A decision tree recursively partitions the feature space by selecting
feature thresholds that maximise class separation (measured by criteria
such as Gini impurity or information gain). Individual trees are fast to
train but prone to overfitting on small datasets. Ensemble methods address
this by combining many trees trained under different conditions.

\subsection{Bagging: Random Forest}

Bootstrap Aggregating (Bagging) trains each tree on a random bootstrap
sample of the training data (sampling with replacement) and restricts
each split to a random subset of features. The final prediction is
the majority vote across all trees. This randomization reduces variance
without substantially increasing bias.

Random Forest is the canonical bagging ensemble. It typically achieves
strong out-of-the-box performance on tabular data but produces large
serialized model files when trained on high-dimensional data with many
trees---a key factor in its caching viability, as discussed in Chapter~4.

\subsection{Boosting: Gradient Boosting and XGBoost}

Boosting trains trees sequentially, where each new tree corrects the
residual errors of the previous ensemble. Gradient Boosting minimises a
differentiable loss function via gradient descent in function space.
XGBoost (Extreme Gradient Boosting) extends this with regularisation
terms, sparsity-aware splitting, and cache-efficient tree construction,
achieving state-of-the-art performance on many tabular benchmarks.

Boosting models typically use shallow trees (depth 3--6) with aggressive
regularisation, producing compact serialized representations. This property
makes them highly cache-viable: a complete XGBoost model for 128-subject
LOSO training serialises to approximately 1--2~MB, compared to
600--1544~MB for a comparable Random Forest.

\subsection{Why Ensembles for EEG Feature Classification}

Ensemble methods are preferred over single models for EEG feature-based
sleep staging for three reasons: (1) the feature space (149 features) is
high-dimensional relative to training set size ($\sim$120{,}000 epochs across
127 subjects per fold), and ensembles regularise effectively; (2) sleep
stage boundaries are non-linear and interact across multiple frequency bands,
which tree-based models capture without feature engineering; and (3) ensemble
methods are interpretable through feature importance scores, enabling
post-hoc analysis of which EEG features most discriminate sleep stages.
